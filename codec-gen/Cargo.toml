[package]
name = "kafgres-codec-gen"
version = "0.1.0"
edition = "2021"
license = "Elastic-2.0"
description = "Emits kafgres-codec from the vendored Kafka message schemas."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
