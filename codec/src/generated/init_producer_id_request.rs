// @generated by kafgres-codec-gen from InitProducerIdRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct InitProducerIdRequest {
    /// The transactional id, or null if the producer is not transactional.
    pub transactional_id: Option<String>,
    /// The time in ms to wait before aborting idle transactions sent by this producer. This
    /// is only relevant if a TransactionalId has been defined.
    pub transaction_timeout_ms: i32,
    /// The producer id. This is used to disambiguate requests if a transactional id is
    /// reused following its expiration.
    pub producer_id: i64,
    /// The producer's current epoch. This will be checked against the producer epoch on the
    /// broker, and the request will return an error if they do not match.
    pub producer_epoch: i16,
    /// True if the client wants to enable two-phase commit (2PC) protocol for transactions.
    pub enable2_pc: bool,
    /// True if the client wants to keep the currently ongoing transaction instead of
    /// aborting it.
    pub keep_prepared_txn: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for InitProducerIdRequest {
    fn default() -> Self {
        Self {
            transactional_id: Some(String::new()),
            transaction_timeout_ms: 0i32,
            producer_id: -1i64,
            producer_epoch: -1i16,
            enable2_pc: false,
            keep_prepared_txn: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for InitProducerIdRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += nullable_string_size(self.transactional_id.as_deref(), _flexible);
        _n += 4;
        if version >= 3 {
            _n += 8;
        }
        if version >= 3 {
            _n += 2;
        }
        if version >= 6 {
            _n += 1;
        }
        if version >= 6 {
            _n += 1;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_nullable_string(buf, self.transactional_id.as_deref(), _flexible)?;
        put_i32(buf, self.transaction_timeout_ms);
        if version >= 3 {
            put_i64(buf, self.producer_id);
        } else {
            debug_assert!(self.producer_id == -1i64, "InitProducerIdRequest.ProducerId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_i16(buf, self.producer_epoch);
        } else {
            debug_assert!(self.producer_epoch == -1i16, "InitProducerIdRequest.ProducerEpoch: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 6 {
            put_bool(buf, self.enable2_pc);
        } else {
            debug_assert!(self.enable2_pc == false, "InitProducerIdRequest.Enable2Pc: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 6 {
            put_bool(buf, self.keep_prepared_txn);
        } else {
            debug_assert!(self.keep_prepared_txn == false, "InitProducerIdRequest.KeepPreparedTxn: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let transactional_id = {
            get_nullable_string(buf, _flexible)?
        };
        let transaction_timeout_ms = {
            get_i32(buf)?
        };
        let producer_id = if version >= 3 {
            get_i64(buf)?
        } else {
            -1i64
        };
        let producer_epoch = if version >= 3 {
            get_i16(buf)?
        } else {
            -1i16
        };
        let enable2_pc = if version >= 6 {
            get_bool(buf)?
        } else {
            false
        };
        let keep_prepared_txn = if version >= 6 {
            get_bool(buf)?
        } else {
            false
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            transactional_id,
            transaction_timeout_ms,
            producer_id,
            producer_epoch,
            enable2_pc,
            keep_prepared_txn,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for InitProducerIdRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.transactional_id = if g.bool() { None } else { Some(g.string()) };
        _s.transaction_timeout_ms = g.i32();
        if version >= 3 {
            _s.producer_id = g.i64();
        }
        if version >= 3 {
            _s.producer_epoch = g.i16();
        }
        if version >= 6 {
            _s.enable2_pc = g.bool();
        }
        if version >= 6 {
            _s.keep_prepared_txn = g.bool();
        }
        _s
    }
}

impl ApiMessage for InitProducerIdRequest {
    const API_KEY: i16 = 22;
    const NAME: &'static str = "InitProducerIdRequest";
    const VALID: VersionRange = VersionRange::new(0, 6);
    const FLEXIBLE: VersionRange = VersionRange::new(2, 32767);
    const LATEST_VERSION_UNSTABLE: bool = true;
}

