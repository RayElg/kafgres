// @generated by kafgres-codec-gen from DescribeClusterResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct DescribeClusterResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The top-level error code, or 0 if there was no error.
    pub error_code: i16,
    /// The top-level error message, or null if there was no error.
    pub error_message: Option<String>,
    /// The endpoint type that was described. 1=brokers, 2=controllers.
    pub endpoint_type: i8,
    /// The cluster ID that responding broker belongs to.
    pub cluster_id: String,
    /// The ID of the controller. When handled by a controller, returns the current voter
    /// leader ID. When handled by a broker, returns a random alive broker ID as a fallback.
    pub controller_id: i32,
    /// Each broker in the response.
    pub brokers: Vec<DescribeClusterBroker>,
    /// 32-bit bitfield to represent authorized operations for this cluster.
    pub cluster_authorized_operations: i32,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribeClusterResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            error_message: None,
            endpoint_type: 1i8,
            cluster_id: String::new(),
            controller_id: -1i32,
            brokers: Vec::<DescribeClusterBroker>::new(),
            cluster_authorized_operations: -2147483648i32,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribeClusterResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += 2;
        _n += nullable_string_size(self.error_message.as_deref(), _flexible);
        if version >= 1 {
            _n += 1;
        }
        _n += string_size(&self.cluster_id, _flexible);
        _n += 4;
        _n += array_len_size(self.brokers.len(), _flexible);
        for _item in self.brokers.iter() {
            _n += _item.size(version);
        }
        _n += 4;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.throttle_time_ms);
        put_i16(buf, self.error_code);
        put_nullable_string(buf, self.error_message.as_deref(), _flexible)?;
        if version >= 1 {
            put_i8(buf, self.endpoint_type);
        } else {
            debug_assert!(self.endpoint_type == 1i8, "DescribeClusterResponse.EndpointType: non-ignorable field set at version {} that cannot carry it", version);
        }
        put_string(buf, &self.cluster_id, _flexible)?;
        put_i32(buf, self.controller_id);
        put_array_len(buf, self.brokers.len(), _flexible)?;
        for _item in self.brokers.iter() {
            _item.encode(buf, version)?;
        }
        put_i32(buf, self.cluster_authorized_operations);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let throttle_time_ms = {
            get_i32(buf)?
        };
        let error_code = {
            get_i16(buf)?
        };
        let error_message = {
            get_nullable_string(buf, _flexible)?
        };
        let endpoint_type = if version >= 1 {
            get_i8(buf)?
        } else {
            1i8
        };
        let cluster_id = {
            get_string(buf, _flexible)?
        };
        let controller_id = {
            get_i32(buf)?
        };
        let brokers = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            DescribeClusterBroker::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let cluster_authorized_operations = {
            get_i32(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            error_message,
            endpoint_type,
            cluster_id,
            controller_id,
            brokers,
            cluster_authorized_operations,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribeClusterResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.throttle_time_ms = g.i32();
        _s.error_code = g.i16();
        _s.error_message = if g.bool() { None } else { Some(g.string()) };
        if version >= 1 {
            _s.endpoint_type = g.i8();
        }
        _s.cluster_id = g.string();
        _s.controller_id = g.i32();
        _s.brokers = { let _k = g.array_len(); (0.._k).map(|_| DescribeClusterBroker::sample(version, g)).collect() };
        _s.cluster_authorized_operations = g.i32();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DescribeClusterBroker {
    /// The broker ID.
    pub broker_id: i32,
    /// The broker hostname.
    pub host: String,
    /// The broker port.
    pub port: i32,
    /// The rack of the broker, or null if it has not been assigned to a rack.
    pub rack: Option<String>,
    /// Whether the broker is fenced
    pub is_fenced: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribeClusterBroker {
    fn default() -> Self {
        Self {
            broker_id: 0i32,
            host: String::new(),
            port: 0i32,
            rack: None,
            is_fenced: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribeClusterBroker {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += string_size(&self.host, _flexible);
        _n += 4;
        _n += nullable_string_size(self.rack.as_deref(), _flexible);
        if version >= 2 {
            _n += 1;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.broker_id);
        put_string(buf, &self.host, _flexible)?;
        put_i32(buf, self.port);
        put_nullable_string(buf, self.rack.as_deref(), _flexible)?;
        if version >= 2 {
            put_bool(buf, self.is_fenced);
        } else {
            debug_assert!(self.is_fenced == false, "DescribeClusterResponse.IsFenced: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let broker_id = {
            get_i32(buf)?
        };
        let host = {
            get_string(buf, _flexible)?
        };
        let port = {
            get_i32(buf)?
        };
        let rack = {
            get_nullable_string(buf, _flexible)?
        };
        let is_fenced = if version >= 2 {
            get_bool(buf)?
        } else {
            false
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            broker_id,
            host,
            port,
            rack,
            is_fenced,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribeClusterBroker {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.broker_id = g.i32();
        _s.host = g.string();
        _s.port = g.i32();
        _s.rack = if g.bool() { None } else { Some(g.string()) };
        if version >= 2 {
            _s.is_fenced = g.bool();
        }
        _s
    }
}

impl ApiMessage for DescribeClusterResponse {
    const API_KEY: i16 = 60;
    const NAME: &'static str = "DescribeClusterResponse";
    const VALID: VersionRange = VersionRange::new(0, 2);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

