// @generated by kafgres-codec-gen from DescribeDelegationTokenResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct DescribeDelegationTokenResponse {
    /// The error code, or 0 if there was no error.
    pub error_code: i16,
    /// The tokens.
    pub tokens: Vec<DescribedDelegationToken>,
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribeDelegationTokenResponse {
    fn default() -> Self {
        Self {
            error_code: 0i16,
            tokens: Vec::<DescribedDelegationToken>::new(),
            throttle_time_ms: 0i32,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribeDelegationTokenResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += 2;
        _n += array_len_size(self.tokens.len(), _flexible);
        for _item in self.tokens.iter() {
            _n += _item.size(version);
        }
        _n += 4;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_i16(buf, self.error_code);
        put_array_len(buf, self.tokens.len(), _flexible)?;
        for _item in self.tokens.iter() {
            _item.encode(buf, version)?;
        }
        put_i32(buf, self.throttle_time_ms);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let error_code = {
            get_i16(buf)?
        };
        let tokens = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            DescribedDelegationToken::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let throttle_time_ms = {
            get_i32(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            error_code,
            tokens,
            throttle_time_ms,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribeDelegationTokenResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.error_code = g.i16();
        _s.tokens = { let _k = g.array_len(); (0.._k).map(|_| DescribedDelegationToken::sample(version, g)).collect() };
        _s.throttle_time_ms = g.i32();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DescribedDelegationToken {
    /// The token principal type.
    pub principal_type: String,
    /// The token principal name.
    pub principal_name: String,
    /// The principal type of the requester of the token.
    pub token_requester_principal_type: String,
    /// The principal type of the requester of the token.
    pub token_requester_principal_name: String,
    /// The token issue timestamp in milliseconds.
    pub issue_timestamp: i64,
    /// The token expiry timestamp in milliseconds.
    pub expiry_timestamp: i64,
    /// The token maximum timestamp length in milliseconds.
    pub max_timestamp: i64,
    /// The token ID.
    pub token_id: String,
    /// The token HMAC.
    pub hmac: Bytes,
    /// Those who are able to renew this token before it expires.
    pub renewers: Vec<DescribedDelegationTokenRenewer>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribedDelegationToken {
    fn default() -> Self {
        Self {
            principal_type: String::new(),
            principal_name: String::new(),
            token_requester_principal_type: String::new(),
            token_requester_principal_name: String::new(),
            issue_timestamp: 0i64,
            expiry_timestamp: 0i64,
            max_timestamp: 0i64,
            token_id: String::new(),
            hmac: Bytes::new(),
            renewers: Vec::<DescribedDelegationTokenRenewer>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribedDelegationToken {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += string_size(&self.principal_type, _flexible);
        _n += string_size(&self.principal_name, _flexible);
        if version >= 3 {
            _n += string_size(&self.token_requester_principal_type, _flexible);
        }
        if version >= 3 {
            _n += string_size(&self.token_requester_principal_name, _flexible);
        }
        _n += 8;
        _n += 8;
        _n += 8;
        _n += string_size(&self.token_id, _flexible);
        _n += bytes_size(&self.hmac, _flexible);
        _n += array_len_size(self.renewers.len(), _flexible);
        for _item in self.renewers.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_string(buf, &self.principal_type, _flexible)?;
        put_string(buf, &self.principal_name, _flexible)?;
        if version >= 3 {
            put_string(buf, &self.token_requester_principal_type, _flexible)?;
        } else {
            debug_assert!(self.token_requester_principal_type == String::new(), "DescribeDelegationTokenResponse.TokenRequesterPrincipalType: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_string(buf, &self.token_requester_principal_name, _flexible)?;
        } else {
            debug_assert!(self.token_requester_principal_name == String::new(), "DescribeDelegationTokenResponse.TokenRequesterPrincipalName: non-ignorable field set at version {} that cannot carry it", version);
        }
        put_i64(buf, self.issue_timestamp);
        put_i64(buf, self.expiry_timestamp);
        put_i64(buf, self.max_timestamp);
        put_string(buf, &self.token_id, _flexible)?;
        put_bytes(buf, &self.hmac, _flexible)?;
        put_array_len(buf, self.renewers.len(), _flexible)?;
        for _item in self.renewers.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let principal_type = {
            get_string(buf, _flexible)?
        };
        let principal_name = {
            get_string(buf, _flexible)?
        };
        let token_requester_principal_type = if version >= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let token_requester_principal_name = if version >= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let issue_timestamp = {
            get_i64(buf)?
        };
        let expiry_timestamp = {
            get_i64(buf)?
        };
        let max_timestamp = {
            get_i64(buf)?
        };
        let token_id = {
            get_string(buf, _flexible)?
        };
        let hmac = {
            get_bytes(buf, _flexible)?
        };
        let renewers = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            DescribedDelegationTokenRenewer::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            principal_type,
            principal_name,
            token_requester_principal_type,
            token_requester_principal_name,
            issue_timestamp,
            expiry_timestamp,
            max_timestamp,
            token_id,
            hmac,
            renewers,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribedDelegationToken {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.principal_type = g.string();
        _s.principal_name = g.string();
        if version >= 3 {
            _s.token_requester_principal_type = g.string();
        }
        if version >= 3 {
            _s.token_requester_principal_name = g.string();
        }
        _s.issue_timestamp = g.i64();
        _s.expiry_timestamp = g.i64();
        _s.max_timestamp = g.i64();
        _s.token_id = g.string();
        _s.hmac = g.bytes();
        _s.renewers = { let _k = g.array_len(); (0.._k).map(|_| DescribedDelegationTokenRenewer::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DescribedDelegationTokenRenewer {
    /// The renewer principal type.
    pub principal_type: String,
    /// The renewer principal name.
    pub principal_name: String,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribedDelegationTokenRenewer {
    fn default() -> Self {
        Self {
            principal_type: String::new(),
            principal_name: String::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribedDelegationTokenRenewer {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += string_size(&self.principal_type, _flexible);
        _n += string_size(&self.principal_name, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_string(buf, &self.principal_type, _flexible)?;
        put_string(buf, &self.principal_name, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let principal_type = {
            get_string(buf, _flexible)?
        };
        let principal_name = {
            get_string(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            principal_type,
            principal_name,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribedDelegationTokenRenewer {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.principal_type = g.string();
        _s.principal_name = g.string();
        _s
    }
}

impl ApiMessage for DescribeDelegationTokenResponse {
    const API_KEY: i16 = 41;
    const NAME: &'static str = "DescribeDelegationTokenResponse";
    const VALID: VersionRange = VersionRange::new(1, 3);
    const FLEXIBLE: VersionRange = VersionRange::new(2, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

