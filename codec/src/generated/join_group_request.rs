// @generated by kafgres-codec-gen from JoinGroupRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct JoinGroupRequest {
    /// The group identifier.
    pub group_id: String,
    /// The coordinator considers the consumer dead if it receives no heartbeat after this
    /// timeout in milliseconds.
    pub session_timeout_ms: i32,
    /// The maximum time in milliseconds that the coordinator will wait for each member to
    /// rejoin when rebalancing the group.
    pub rebalance_timeout_ms: i32,
    /// The member id assigned by the group coordinator.
    pub member_id: String,
    /// The unique identifier of the consumer instance provided by end user.
    pub group_instance_id: Option<String>,
    /// The unique name the for class of protocols implemented by the group we want to join.
    pub protocol_type: String,
    /// The list of protocols that the member supports.
    pub protocols: Vec<JoinGroupRequestProtocol>,
    /// The reason why the member (re-)joins the group.
    pub reason: Option<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for JoinGroupRequest {
    fn default() -> Self {
        Self {
            group_id: String::new(),
            session_timeout_ms: 0i32,
            rebalance_timeout_ms: -1i32,
            member_id: String::new(),
            group_instance_id: None,
            protocol_type: String::new(),
            protocols: Vec::<JoinGroupRequestProtocol>::new(),
            reason: None,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for JoinGroupRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 6;
        let mut _n = 0usize;
        _n += string_size(&self.group_id, _flexible);
        _n += 4;
        if version >= 1 {
            _n += 4;
        }
        _n += string_size(&self.member_id, _flexible);
        if version >= 5 {
            if version >= 5 {
                _n += nullable_string_size(self.group_instance_id.as_deref(), _flexible);
            } else if let Some(_v) = &self.group_instance_id {
                _n += string_size(&_v, _flexible);
            }
        }
        _n += string_size(&self.protocol_type, _flexible);
        _n += array_len_size(self.protocols.len(), _flexible);
        for _item in self.protocols.iter() {
            _n += _item.size(version);
        }
        if version >= 8 {
            if version >= 8 {
                _n += nullable_string_size(self.reason.as_deref(), _flexible);
            } else if let Some(_v) = &self.reason {
                _n += string_size(&_v, _flexible);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 6;
        put_string(buf, &self.group_id, _flexible)?;
        put_i32(buf, self.session_timeout_ms);
        if version >= 1 {
            put_i32(buf, self.rebalance_timeout_ms);
        }
        put_string(buf, &self.member_id, _flexible)?;
        if version >= 5 {
            if version >= 5 {
                put_nullable_string(buf, self.group_instance_id.as_deref(), _flexible)?;
            } else {
                let _v = self.group_instance_id.as_ref().ok_or(CodecError::UnexpectedNull("GroupInstanceId"))?;
                put_string(buf, &_v, _flexible)?;
            }
        } else {
            debug_assert!(self.group_instance_id == None, "JoinGroupRequest.GroupInstanceId: non-ignorable field set at version {} that cannot carry it", version);
        }
        put_string(buf, &self.protocol_type, _flexible)?;
        put_array_len(buf, self.protocols.len(), _flexible)?;
        for _item in self.protocols.iter() {
            _item.encode(buf, version)?;
        }
        if version >= 8 {
            if version >= 8 {
                put_nullable_string(buf, self.reason.as_deref(), _flexible)?;
            } else {
                let _v = self.reason.as_ref().ok_or(CodecError::UnexpectedNull("Reason"))?;
                put_string(buf, &_v, _flexible)?;
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 6;
        let group_id = {
            get_string(buf, _flexible)?
        };
        let session_timeout_ms = {
            get_i32(buf)?
        };
        let rebalance_timeout_ms = if version >= 1 {
            get_i32(buf)?
        } else {
            -1i32
        };
        let member_id = {
            get_string(buf, _flexible)?
        };
        let group_instance_id = if version >= 5 {
            if version >= 5 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let protocol_type = {
            get_string(buf, _flexible)?
        };
        let protocols = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            JoinGroupRequestProtocol::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let reason = if version >= 8 {
            if version >= 8 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            group_id,
            session_timeout_ms,
            rebalance_timeout_ms,
            member_id,
            group_instance_id,
            protocol_type,
            protocols,
            reason,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for JoinGroupRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 6;
        let mut _s = Self::default();
        _s.group_id = g.string();
        _s.session_timeout_ms = g.i32();
        if version >= 1 {
            _s.rebalance_timeout_ms = g.i32();
        }
        _s.member_id = g.string();
        if version >= 5 {
            _s.group_instance_id = if version >= 5 && g.bool() { None } else { Some(g.string()) };
        }
        _s.protocol_type = g.string();
        _s.protocols = { let _k = g.array_len(); (0.._k).map(|_| JoinGroupRequestProtocol::sample(version, g)).collect() };
        if version >= 8 {
            _s.reason = if version >= 8 && g.bool() { None } else { Some(g.string()) };
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct JoinGroupRequestProtocol {
    /// The protocol name.
    pub name: String,
    /// The protocol metadata.
    pub metadata: Bytes,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for JoinGroupRequestProtocol {
    fn default() -> Self {
        Self {
            name: String::new(),
            metadata: Bytes::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for JoinGroupRequestProtocol {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 6;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += bytes_size(&self.metadata, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 6;
        put_string(buf, &self.name, _flexible)?;
        put_bytes(buf, &self.metadata, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 6;
        let name = {
            get_string(buf, _flexible)?
        };
        let metadata = {
            get_bytes(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            metadata,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for JoinGroupRequestProtocol {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 6;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.metadata = g.bytes();
        _s
    }
}

impl ApiMessage for JoinGroupRequest {
    const API_KEY: i16 = 11;
    const NAME: &'static str = "JoinGroupRequest";
    const VALID: VersionRange = VersionRange::new(0, 9);
    const FLEXIBLE: VersionRange = VersionRange::new(6, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

