// @generated by kafgres-codec-gen from UpdateFeaturesRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct UpdateFeaturesRequest {
    /// How long to wait in milliseconds before timing out the request.
    pub timeout_ms: i32,
    /// The list of updates to finalized features.
    pub feature_updates: Vec<FeatureUpdateKey>,
    /// True if we should validate the request, but not perform the upgrade or downgrade.
    pub validate_only: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for UpdateFeaturesRequest {
    fn default() -> Self {
        Self {
            timeout_ms: 60000i32,
            feature_updates: Vec::<FeatureUpdateKey>::new(),
            validate_only: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for UpdateFeaturesRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += array_len_size(self.feature_updates.len(), _flexible);
        for _item in self.feature_updates.iter() {
            _n += _item.size(version);
        }
        if version >= 1 {
            _n += 1;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.timeout_ms);
        put_array_len(buf, self.feature_updates.len(), _flexible)?;
        for _item in self.feature_updates.iter() {
            _item.encode(buf, version)?;
        }
        if version >= 1 {
            put_bool(buf, self.validate_only);
        } else {
            debug_assert!(self.validate_only == false, "UpdateFeaturesRequest.ValidateOnly: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let timeout_ms = {
            get_i32(buf)?
        };
        let feature_updates = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            FeatureUpdateKey::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let validate_only = if version >= 1 {
            get_bool(buf)?
        } else {
            false
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            timeout_ms,
            feature_updates,
            validate_only,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for UpdateFeaturesRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.timeout_ms = g.i32();
        _s.feature_updates = { let _k = g.array_len(); (0.._k).map(|_| FeatureUpdateKey::sample(version, g)).collect() };
        if version >= 1 {
            _s.validate_only = g.bool();
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct FeatureUpdateKey {
    /// The name of the finalized feature to be updated.
    pub feature: String,
    /// The new maximum version level for the finalized feature. A value >= 1 is valid. A
    /// value < 1, is special, and can be used to request the deletion of the finalized
    /// feature.
    pub max_version_level: i16,
    /// DEPRECATED in version 1 (see DowngradeType). When set to true, the finalized feature
    /// version level is allowed to be downgraded/deleted. The downgrade request will fail
    /// if the new maximum version level is a value that's not lower than the existing
    /// maximum finalized version level.
    pub allow_downgrade: bool,
    /// Determine which type of upgrade will be performed: 1 will perform an upgrade only
    /// (default), 2 is safe downgrades only (lossless), 3 is unsafe downgrades (lossy).
    pub upgrade_type: i8,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for FeatureUpdateKey {
    fn default() -> Self {
        Self {
            feature: String::new(),
            max_version_level: 0i16,
            allow_downgrade: false,
            upgrade_type: 1i8,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for FeatureUpdateKey {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.feature, _flexible);
        _n += 2;
        if version <= 0 {
            _n += 1;
        }
        if version >= 1 {
            _n += 1;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.feature, _flexible)?;
        put_i16(buf, self.max_version_level);
        if version <= 0 {
            put_bool(buf, self.allow_downgrade);
        } else {
            debug_assert!(self.allow_downgrade == false, "UpdateFeaturesRequest.AllowDowngrade: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 1 {
            put_i8(buf, self.upgrade_type);
        } else {
            debug_assert!(self.upgrade_type == 1i8, "UpdateFeaturesRequest.UpgradeType: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let feature = {
            get_string(buf, _flexible)?
        };
        let max_version_level = {
            get_i16(buf)?
        };
        let allow_downgrade = if version <= 0 {
            get_bool(buf)?
        } else {
            false
        };
        let upgrade_type = if version >= 1 {
            get_i8(buf)?
        } else {
            1i8
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            feature,
            max_version_level,
            allow_downgrade,
            upgrade_type,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for FeatureUpdateKey {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.feature = g.string();
        _s.max_version_level = g.i16();
        if version <= 0 {
            _s.allow_downgrade = g.bool();
        }
        if version >= 1 {
            _s.upgrade_type = g.i8();
        }
        _s
    }
}

impl ApiMessage for UpdateFeaturesRequest {
    const API_KEY: i16 = 57;
    const NAME: &'static str = "UpdateFeaturesRequest";
    const VALID: VersionRange = VersionRange::new(0, 2);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

