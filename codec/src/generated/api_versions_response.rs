// @generated by kafgres-codec-gen from ApiVersionsResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct ApiVersionsResponse {
    /// The top-level error code.
    pub error_code: i16,
    /// The APIs supported by the broker.
    pub api_keys: Vec<ApiVersion>,
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// Features supported by the broker. Note: in v0-v3, features with MinSupportedVersion
    /// = 0 are omitted.
    pub supported_features: Vec<SupportedFeatureKey>,
    /// The monotonically increasing epoch for the finalized features information. Valid
    /// values are >= 0. A value of -1 is special and represents unknown epoch.
    pub finalized_features_epoch: i64,
    /// List of cluster-wide finalized features. The information is valid only if
    /// FinalizedFeaturesEpoch >= 0.
    pub finalized_features: Vec<FinalizedFeatureKey>,
    /// Set by a KRaft controller if the required configurations for ZK migration are
    /// present.
    pub zk_migration_ready: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for ApiVersionsResponse {
    fn default() -> Self {
        Self {
            error_code: 0i16,
            api_keys: Vec::<ApiVersion>::new(),
            throttle_time_ms: 0i32,
            supported_features: Vec::<SupportedFeatureKey>::new(),
            finalized_features_epoch: -1i64,
            finalized_features: Vec::<FinalizedFeatureKey>::new(),
            zk_migration_ready: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for ApiVersionsResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += 2;
        _n += array_len_size(self.api_keys.len(), _flexible);
        for _item in self.api_keys.iter() {
            _n += _item.size(version);
        }
        if version >= 1 {
            _n += 4;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            if version >= 3 && self.supported_features != Vec::<SupportedFeatureKey>::new() { _num_tagged += 1; }
            if version >= 3 && self.finalized_features_epoch != -1i64 { _num_tagged += 1; }
            if version >= 3 && self.finalized_features != Vec::<FinalizedFeatureKey>::new() { _num_tagged += 1; }
            if version >= 3 && self.zk_migration_ready != false { _num_tagged += 1; }
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
            if version >= 3 && self.supported_features != Vec::<SupportedFeatureKey>::new() {
                let _outer = _n;
                _n = 0;
                _n += array_len_size(self.supported_features.len(), _flexible);
                for _item in self.supported_features.iter() {
                    _n += _item.size(version);
                }
                let _sz = _n;
                _n = _outer + uvarint_size(0) + uvarint_size(_sz as u32) + _sz;
            }
            if version >= 3 && self.finalized_features_epoch != -1i64 {
                let _outer = _n;
                _n = 0;
                _n += 8;
                let _sz = _n;
                _n = _outer + uvarint_size(1) + uvarint_size(_sz as u32) + _sz;
            }
            if version >= 3 && self.finalized_features != Vec::<FinalizedFeatureKey>::new() {
                let _outer = _n;
                _n = 0;
                _n += array_len_size(self.finalized_features.len(), _flexible);
                for _item in self.finalized_features.iter() {
                    _n += _item.size(version);
                }
                let _sz = _n;
                _n = _outer + uvarint_size(2) + uvarint_size(_sz as u32) + _sz;
            }
            if version >= 3 && self.zk_migration_ready != false {
                let _outer = _n;
                _n = 0;
                _n += 1;
                let _sz = _n;
                _n = _outer + uvarint_size(3) + uvarint_size(_sz as u32) + _sz;
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_i16(buf, self.error_code);
        put_array_len(buf, self.api_keys.len(), _flexible)?;
        for _item in self.api_keys.iter() {
            _item.encode(buf, version)?;
        }
        if version >= 1 {
            put_i32(buf, self.throttle_time_ms);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            if version >= 3 && self.supported_features != Vec::<SupportedFeatureKey>::new() { _num_tagged += 1; }
            if version >= 3 && self.finalized_features_epoch != -1i64 { _num_tagged += 1; }
            if version >= 3 && self.finalized_features != Vec::<FinalizedFeatureKey>::new() { _num_tagged += 1; }
            if version >= 3 && self.zk_migration_ready != false { _num_tagged += 1; }
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            if version >= 3 && self.supported_features != Vec::<SupportedFeatureKey>::new() {
                put_uvarint(buf, 0);
                let _sz = {
                    let mut _n = 0usize;
                    _n += array_len_size(self.supported_features.len(), _flexible);
                    for _item in self.supported_features.iter() {
                        _n += _item.size(version);
                    }
                    _n
                };
                put_uvarint(buf, _sz as u32);
                put_array_len(buf, self.supported_features.len(), _flexible)?;
                for _item in self.supported_features.iter() {
                    _item.encode(buf, version)?;
                }
            }
            while _ui < self.unknown_tagged_fields.len() && self.unknown_tagged_fields[_ui].tag < 1 {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
            if version >= 3 && self.finalized_features_epoch != -1i64 {
                put_uvarint(buf, 1);
                let _sz = {
                    let mut _n = 0usize;
                    _n += 8;
                    _n
                };
                put_uvarint(buf, _sz as u32);
                put_i64(buf, self.finalized_features_epoch);
            }
            while _ui < self.unknown_tagged_fields.len() && self.unknown_tagged_fields[_ui].tag < 2 {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
            if version >= 3 && self.finalized_features != Vec::<FinalizedFeatureKey>::new() {
                put_uvarint(buf, 2);
                let _sz = {
                    let mut _n = 0usize;
                    _n += array_len_size(self.finalized_features.len(), _flexible);
                    for _item in self.finalized_features.iter() {
                        _n += _item.size(version);
                    }
                    _n
                };
                put_uvarint(buf, _sz as u32);
                put_array_len(buf, self.finalized_features.len(), _flexible)?;
                for _item in self.finalized_features.iter() {
                    _item.encode(buf, version)?;
                }
            }
            while _ui < self.unknown_tagged_fields.len() && self.unknown_tagged_fields[_ui].tag < 3 {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
            if version >= 3 && self.zk_migration_ready != false {
                put_uvarint(buf, 3);
                let _sz = {
                    let mut _n = 0usize;
                    _n += 1;
                    _n
                };
                put_uvarint(buf, _sz as u32);
                put_bool(buf, self.zk_migration_ready);
            }
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let error_code = {
            get_i16(buf)?
        };
        let api_keys = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            ApiVersion::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let throttle_time_ms = if version >= 1 {
            get_i32(buf)?
        } else {
            0i32
        };
        let mut supported_features = Vec::<SupportedFeatureKey>::new();
        let mut finalized_features_epoch = -1i64;
        let mut finalized_features = Vec::<FinalizedFeatureKey>::new();
        let mut zk_migration_ready = false;
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    0 => {
                        let buf = &mut _sub;
                        supported_features = {
                            match get_array_len(buf, _flexible)? {
                                None => return Err(CodecError::UnexpectedNull("array")),
                                Some(_len) => {
                                    let mut _v = alloc_vec(_len);
                                    for _ in 0.._len {
                                        _v.push({
                                            SupportedFeatureKey::decode(buf, version)?
                                        });
                                    }
                                    _v
                                }
                            }
                        };
                    }
                    1 => {
                        let buf = &mut _sub;
                        finalized_features_epoch = {
                            get_i64(buf)?
                        };
                    }
                    2 => {
                        let buf = &mut _sub;
                        finalized_features = {
                            match get_array_len(buf, _flexible)? {
                                None => return Err(CodecError::UnexpectedNull("array")),
                                Some(_len) => {
                                    let mut _v = alloc_vec(_len);
                                    for _ in 0.._len {
                                        _v.push({
                                            FinalizedFeatureKey::decode(buf, version)?
                                        });
                                    }
                                    _v
                                }
                            }
                        };
                    }
                    3 => {
                        let buf = &mut _sub;
                        zk_migration_ready = {
                            get_bool(buf)?
                        };
                    }
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            error_code,
            api_keys,
            throttle_time_ms,
            supported_features,
            finalized_features_epoch,
            finalized_features,
            zk_migration_ready,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for ApiVersionsResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.error_code = g.i16();
        _s.api_keys = { let _k = g.array_len(); (0.._k).map(|_| ApiVersion::sample(version, g)).collect() };
        if version >= 1 {
            _s.throttle_time_ms = g.i32();
        }
        if _flexible && version >= 3 {
            _s.supported_features = { let _k = g.array_len(); (0.._k).map(|_| SupportedFeatureKey::sample(version, g)).collect() };
        }
        if _flexible && version >= 3 {
            _s.finalized_features_epoch = g.i64();
        }
        if _flexible && version >= 3 {
            _s.finalized_features = { let _k = g.array_len(); (0.._k).map(|_| FinalizedFeatureKey::sample(version, g)).collect() };
        }
        if _flexible && version >= 3 {
            _s.zk_migration_ready = g.bool();
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct ApiVersion {
    /// The API index.
    pub api_key: i16,
    /// The minimum supported version, inclusive.
    pub min_version: i16,
    /// The maximum supported version, inclusive.
    pub max_version: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for ApiVersion {
    fn default() -> Self {
        Self {
            api_key: 0i16,
            min_version: 0i16,
            max_version: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for ApiVersion {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += 2;
        _n += 2;
        _n += 2;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_i16(buf, self.api_key);
        put_i16(buf, self.min_version);
        put_i16(buf, self.max_version);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let api_key = {
            get_i16(buf)?
        };
        let min_version = {
            get_i16(buf)?
        };
        let max_version = {
            get_i16(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            api_key,
            min_version,
            max_version,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for ApiVersion {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.api_key = g.i16();
        _s.min_version = g.i16();
        _s.max_version = g.i16();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct SupportedFeatureKey {
    /// The name of the feature.
    pub name: String,
    /// The minimum supported version for the feature.
    pub min_version: i16,
    /// The maximum supported version for the feature.
    pub max_version: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for SupportedFeatureKey {
    fn default() -> Self {
        Self {
            name: String::new(),
            min_version: 0i16,
            max_version: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for SupportedFeatureKey {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 3 {
            _n += string_size(&self.name, _flexible);
        }
        if version >= 3 {
            _n += 2;
        }
        if version >= 3 {
            _n += 2;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 3 {
            put_string(buf, &self.name, _flexible)?;
        } else {
            debug_assert!(self.name == String::new(), "ApiVersionsResponse.Name: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_i16(buf, self.min_version);
        } else {
            debug_assert!(self.min_version == 0i16, "ApiVersionsResponse.MinVersion: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_i16(buf, self.max_version);
        } else {
            debug_assert!(self.max_version == 0i16, "ApiVersionsResponse.MaxVersion: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let name = if version >= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let min_version = if version >= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let max_version = if version >= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            min_version,
            max_version,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for SupportedFeatureKey {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 3 {
            _s.name = g.string();
        }
        if version >= 3 {
            _s.min_version = g.i16();
        }
        if version >= 3 {
            _s.max_version = g.i16();
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct FinalizedFeatureKey {
    /// The name of the feature.
    pub name: String,
    /// The cluster-wide finalized max version level for the feature.
    pub max_version_level: i16,
    /// The cluster-wide finalized min version level for the feature.
    pub min_version_level: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for FinalizedFeatureKey {
    fn default() -> Self {
        Self {
            name: String::new(),
            max_version_level: 0i16,
            min_version_level: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for FinalizedFeatureKey {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 3 {
            _n += string_size(&self.name, _flexible);
        }
        if version >= 3 {
            _n += 2;
        }
        if version >= 3 {
            _n += 2;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 3 {
            put_string(buf, &self.name, _flexible)?;
        } else {
            debug_assert!(self.name == String::new(), "ApiVersionsResponse.Name: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_i16(buf, self.max_version_level);
        } else {
            debug_assert!(self.max_version_level == 0i16, "ApiVersionsResponse.MaxVersionLevel: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            put_i16(buf, self.min_version_level);
        } else {
            debug_assert!(self.min_version_level == 0i16, "ApiVersionsResponse.MinVersionLevel: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let name = if version >= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let max_version_level = if version >= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let min_version_level = if version >= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            max_version_level,
            min_version_level,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for FinalizedFeatureKey {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 3 {
            _s.name = g.string();
        }
        if version >= 3 {
            _s.max_version_level = g.i16();
        }
        if version >= 3 {
            _s.min_version_level = g.i16();
        }
        _s
    }
}

impl ApiMessage for ApiVersionsResponse {
    const API_KEY: i16 = 18;
    const NAME: &'static str = "ApiVersionsResponse";
    const VALID: VersionRange = VersionRange::new(0, 4);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

