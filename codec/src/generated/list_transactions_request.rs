// @generated by kafgres-codec-gen from ListTransactionsRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct ListTransactionsRequest {
    /// The transaction states to filter by: if empty, all transactions are returned; if
    /// non-empty, then only transactions matching one of the filtered states will be
    /// returned.
    pub state_filters: Vec<String>,
    /// The producerIds to filter by: if empty, all transactions will be returned; if
    /// non-empty, only transactions which match one of the filtered producerIds will be
    /// returned.
    pub producer_id_filters: Vec<i64>,
    /// Duration (in millis) to filter by: if < 0, all transactions will be returned;
    /// otherwise, only transactions running longer than this duration will be returned.
    pub duration_filter: i64,
    /// The transactional ID regular expression pattern to filter by: if it is empty or
    /// null, all transactions are returned; Otherwise then only the transactions matching
    /// the given regular expression will be returned.
    pub transactional_id_pattern: Option<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for ListTransactionsRequest {
    fn default() -> Self {
        Self {
            state_filters: Vec::<String>::new(),
            producer_id_filters: Vec::<i64>::new(),
            duration_filter: -1i64,
            transactional_id_pattern: None,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for ListTransactionsRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += array_len_size(self.state_filters.len(), _flexible);
        for _item in self.state_filters.iter() {
            _n += string_size(&_item, _flexible);
        }
        _n += array_len_size(self.producer_id_filters.len(), _flexible);
        for _item in self.producer_id_filters.iter() {
            _n += 8;
        }
        if version >= 1 {
            _n += 8;
        }
        if version >= 2 {
            if version >= 2 {
                _n += nullable_string_size(self.transactional_id_pattern.as_deref(), _flexible);
            } else if let Some(_v) = &self.transactional_id_pattern {
                _n += string_size(&_v, _flexible);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_array_len(buf, self.state_filters.len(), _flexible)?;
        for _item in self.state_filters.iter() {
            put_string(buf, &_item, _flexible)?;
        }
        put_array_len(buf, self.producer_id_filters.len(), _flexible)?;
        for _item in self.producer_id_filters.iter() {
            put_i64(buf, *_item);
        }
        if version >= 1 {
            put_i64(buf, self.duration_filter);
        } else {
            debug_assert!(self.duration_filter == -1i64, "ListTransactionsRequest.DurationFilter: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 2 {
            if version >= 2 {
                put_nullable_string(buf, self.transactional_id_pattern.as_deref(), _flexible)?;
            } else {
                let _v = self.transactional_id_pattern.as_ref().ok_or(CodecError::UnexpectedNull("TransactionalIdPattern"))?;
                put_string(buf, &_v, _flexible)?;
            }
        } else {
            debug_assert!(self.transactional_id_pattern == None, "ListTransactionsRequest.TransactionalIdPattern: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let state_filters = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        };
        let producer_id_filters = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i64(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let duration_filter = if version >= 1 {
            get_i64(buf)?
        } else {
            -1i64
        };
        let transactional_id_pattern = if version >= 2 {
            if version >= 2 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            state_filters,
            producer_id_filters,
            duration_filter,
            transactional_id_pattern,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for ListTransactionsRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.state_filters = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        _s.producer_id_filters = { let _k = g.array_len(); (0.._k).map(|_| g.i64()).collect() };
        if version >= 1 {
            _s.duration_filter = g.i64();
        }
        if version >= 2 {
            _s.transactional_id_pattern = if version >= 2 && g.bool() { None } else { Some(g.string()) };
        }
        _s
    }
}

impl ApiMessage for ListTransactionsRequest {
    const API_KEY: i16 = 66;
    const NAME: &'static str = "ListTransactionsRequest";
    const VALID: VersionRange = VersionRange::new(0, 2);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

