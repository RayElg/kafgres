// @generated by kafgres-codec-gen from DeleteAclsRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct DeleteAclsRequest {
    /// The filters to use when deleting ACLs.
    pub filters: Vec<DeleteAclsFilter>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DeleteAclsRequest {
    fn default() -> Self {
        Self {
            filters: Vec::<DeleteAclsFilter>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DeleteAclsRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += array_len_size(self.filters.len(), _flexible);
        for _item in self.filters.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_array_len(buf, self.filters.len(), _flexible)?;
        for _item in self.filters.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let filters = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            DeleteAclsFilter::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            filters,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DeleteAclsRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.filters = { let _k = g.array_len(); (0.._k).map(|_| DeleteAclsFilter::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DeleteAclsFilter {
    /// The resource type.
    pub resource_type_filter: i8,
    /// The resource name, or null to match any resource name.
    pub resource_name_filter: Option<String>,
    /// The pattern type.
    pub pattern_type_filter: i8,
    /// The principal filter, or null to accept all principals.
    pub principal_filter: Option<String>,
    /// The host filter, or null to accept all hosts.
    pub host_filter: Option<String>,
    /// The ACL operation.
    pub operation: i8,
    /// The permission type.
    pub permission_type: i8,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DeleteAclsFilter {
    fn default() -> Self {
        Self {
            resource_type_filter: 0i8,
            resource_name_filter: Some(String::new()),
            pattern_type_filter: 3i8,
            principal_filter: Some(String::new()),
            host_filter: Some(String::new()),
            operation: 0i8,
            permission_type: 0i8,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DeleteAclsFilter {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += 1;
        _n += nullable_string_size(self.resource_name_filter.as_deref(), _flexible);
        _n += 1;
        _n += nullable_string_size(self.principal_filter.as_deref(), _flexible);
        _n += nullable_string_size(self.host_filter.as_deref(), _flexible);
        _n += 1;
        _n += 1;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_i8(buf, self.resource_type_filter);
        put_nullable_string(buf, self.resource_name_filter.as_deref(), _flexible)?;
        put_i8(buf, self.pattern_type_filter);
        put_nullable_string(buf, self.principal_filter.as_deref(), _flexible)?;
        put_nullable_string(buf, self.host_filter.as_deref(), _flexible)?;
        put_i8(buf, self.operation);
        put_i8(buf, self.permission_type);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let resource_type_filter = {
            get_i8(buf)?
        };
        let resource_name_filter = {
            get_nullable_string(buf, _flexible)?
        };
        let pattern_type_filter = {
            get_i8(buf)?
        };
        let principal_filter = {
            get_nullable_string(buf, _flexible)?
        };
        let host_filter = {
            get_nullable_string(buf, _flexible)?
        };
        let operation = {
            get_i8(buf)?
        };
        let permission_type = {
            get_i8(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            resource_type_filter,
            resource_name_filter,
            pattern_type_filter,
            principal_filter,
            host_filter,
            operation,
            permission_type,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DeleteAclsFilter {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.resource_type_filter = g.i8();
        _s.resource_name_filter = if g.bool() { None } else { Some(g.string()) };
        _s.pattern_type_filter = g.i8();
        _s.principal_filter = if g.bool() { None } else { Some(g.string()) };
        _s.host_filter = if g.bool() { None } else { Some(g.string()) };
        _s.operation = g.i8();
        _s.permission_type = g.i8();
        _s
    }
}

impl ApiMessage for DeleteAclsRequest {
    const API_KEY: i16 = 31;
    const NAME: &'static str = "DeleteAclsRequest";
    const VALID: VersionRange = VersionRange::new(1, 3);
    const FLEXIBLE: VersionRange = VersionRange::new(2, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

