// @generated by kafgres-codec-gen from JoinGroupResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct JoinGroupResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The error code, or 0 if there was no error.
    pub error_code: i16,
    /// The generation ID of the group.
    pub generation_id: i32,
    /// The group protocol name.
    pub protocol_type: Option<String>,
    /// The group protocol selected by the coordinator.
    pub protocol_name: Option<String>,
    /// The leader of the group.
    pub leader: String,
    /// True if the leader must skip running the assignment.
    pub skip_assignment: bool,
    /// The member ID assigned by the group coordinator.
    pub member_id: String,
    /// The group members.
    pub members: Vec<JoinGroupResponseMember>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for JoinGroupResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            generation_id: -1i32,
            protocol_type: None,
            protocol_name: Some(String::new()),
            leader: String::new(),
            skip_assignment: false,
            member_id: String::new(),
            members: Vec::<JoinGroupResponseMember>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for JoinGroupResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 6;
        let mut _n = 0usize;
        if version >= 2 {
            _n += 4;
        }
        _n += 2;
        _n += 4;
        if version >= 7 {
            if version >= 7 {
                _n += nullable_string_size(self.protocol_type.as_deref(), _flexible);
            } else if let Some(_v) = &self.protocol_type {
                _n += string_size(&_v, _flexible);
            }
        }
        if version >= 7 {
            _n += nullable_string_size(self.protocol_name.as_deref(), _flexible);
        } else if let Some(_v) = &self.protocol_name {
            _n += string_size(&_v, _flexible);
        }
        _n += string_size(&self.leader, _flexible);
        if version >= 9 {
            _n += 1;
        }
        _n += string_size(&self.member_id, _flexible);
        _n += array_len_size(self.members.len(), _flexible);
        for _item in self.members.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 6;
        if version >= 2 {
            put_i32(buf, self.throttle_time_ms);
        }
        put_i16(buf, self.error_code);
        put_i32(buf, self.generation_id);
        if version >= 7 {
            if version >= 7 {
                put_nullable_string(buf, self.protocol_type.as_deref(), _flexible)?;
            } else {
                let _v = self.protocol_type.as_ref().ok_or(CodecError::UnexpectedNull("ProtocolType"))?;
                put_string(buf, &_v, _flexible)?;
            }
        }
        if version >= 7 {
            put_nullable_string(buf, self.protocol_name.as_deref(), _flexible)?;
        } else {
            let _v = self.protocol_name.as_ref().ok_or(CodecError::UnexpectedNull("ProtocolName"))?;
            put_string(buf, &_v, _flexible)?;
        }
        put_string(buf, &self.leader, _flexible)?;
        if version >= 9 {
            put_bool(buf, self.skip_assignment);
        } else {
            debug_assert!(self.skip_assignment == false, "JoinGroupResponse.SkipAssignment: non-ignorable field set at version {} that cannot carry it", version);
        }
        put_string(buf, &self.member_id, _flexible)?;
        put_array_len(buf, self.members.len(), _flexible)?;
        for _item in self.members.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 6;
        let throttle_time_ms = if version >= 2 {
            get_i32(buf)?
        } else {
            0i32
        };
        let error_code = {
            get_i16(buf)?
        };
        let generation_id = {
            get_i32(buf)?
        };
        let protocol_type = if version >= 7 {
            if version >= 7 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let protocol_name = {
            if version >= 7 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        };
        let leader = {
            get_string(buf, _flexible)?
        };
        let skip_assignment = if version >= 9 {
            get_bool(buf)?
        } else {
            false
        };
        let member_id = {
            get_string(buf, _flexible)?
        };
        let members = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            JoinGroupResponseMember::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            generation_id,
            protocol_type,
            protocol_name,
            leader,
            skip_assignment,
            member_id,
            members,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for JoinGroupResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 6;
        let mut _s = Self::default();
        if version >= 2 {
            _s.throttle_time_ms = g.i32();
        }
        _s.error_code = g.i16();
        _s.generation_id = g.i32();
        if version >= 7 {
            _s.protocol_type = if version >= 7 && g.bool() { None } else { Some(g.string()) };
        }
        _s.protocol_name = if version >= 7 && g.bool() { None } else { Some(g.string()) };
        _s.leader = g.string();
        if version >= 9 {
            _s.skip_assignment = g.bool();
        }
        _s.member_id = g.string();
        _s.members = { let _k = g.array_len(); (0.._k).map(|_| JoinGroupResponseMember::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct JoinGroupResponseMember {
    /// The group member ID.
    pub member_id: String,
    /// The unique identifier of the consumer instance provided by end user.
    pub group_instance_id: Option<String>,
    /// The group member metadata.
    pub metadata: Bytes,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for JoinGroupResponseMember {
    fn default() -> Self {
        Self {
            member_id: String::new(),
            group_instance_id: None,
            metadata: Bytes::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for JoinGroupResponseMember {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 6;
        let mut _n = 0usize;
        _n += string_size(&self.member_id, _flexible);
        if version >= 5 {
            if version >= 5 {
                _n += nullable_string_size(self.group_instance_id.as_deref(), _flexible);
            } else if let Some(_v) = &self.group_instance_id {
                _n += string_size(&_v, _flexible);
            }
        }
        _n += bytes_size(&self.metadata, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 6;
        put_string(buf, &self.member_id, _flexible)?;
        if version >= 5 {
            if version >= 5 {
                put_nullable_string(buf, self.group_instance_id.as_deref(), _flexible)?;
            } else {
                let _v = self.group_instance_id.as_ref().ok_or(CodecError::UnexpectedNull("GroupInstanceId"))?;
                put_string(buf, &_v, _flexible)?;
            }
        }
        put_bytes(buf, &self.metadata, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 6;
        let member_id = {
            get_string(buf, _flexible)?
        };
        let group_instance_id = if version >= 5 {
            if version >= 5 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let metadata = {
            get_bytes(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            member_id,
            group_instance_id,
            metadata,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for JoinGroupResponseMember {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 6;
        let mut _s = Self::default();
        _s.member_id = g.string();
        if version >= 5 {
            _s.group_instance_id = if version >= 5 && g.bool() { None } else { Some(g.string()) };
        }
        _s.metadata = g.bytes();
        _s
    }
}

impl ApiMessage for JoinGroupResponse {
    const API_KEY: i16 = 11;
    const NAME: &'static str = "JoinGroupResponse";
    const VALID: VersionRange = VersionRange::new(0, 9);
    const FLEXIBLE: VersionRange = VersionRange::new(6, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

