// @generated by kafgres-codec-gen from AddPartitionsToTxnResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnResponse {
    /// Duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The response top level error code.
    pub error_code: i16,
    /// Results categorized by transactional ID.
    pub results_by_transaction: Vec<AddPartitionsToTxnResult>,
    /// The results for each topic.
    pub results_by_topic_v3_and_below: Vec<AddPartitionsToTxnTopicResult>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            results_by_transaction: Vec::<AddPartitionsToTxnResult>::new(),
            results_by_topic_v3_and_below: Vec::<AddPartitionsToTxnTopicResult>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += 4;
        if version >= 4 {
            _n += 2;
        }
        if version >= 4 {
            _n += array_len_size(self.results_by_transaction.len(), _flexible);
            for _item in self.results_by_transaction.iter() {
                _n += _item.size(version);
            }
        }
        if version <= 3 {
            _n += array_len_size(self.results_by_topic_v3_and_below.len(), _flexible);
            for _item in self.results_by_topic_v3_and_below.iter() {
                _n += _item.size(version);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_i32(buf, self.throttle_time_ms);
        if version >= 4 {
            put_i16(buf, self.error_code);
        }
        if version >= 4 {
            put_array_len(buf, self.results_by_transaction.len(), _flexible)?;
            for _item in self.results_by_transaction.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.results_by_transaction == Vec::<AddPartitionsToTxnResult>::new(), "AddPartitionsToTxnResponse.ResultsByTransaction: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_array_len(buf, self.results_by_topic_v3_and_below.len(), _flexible)?;
            for _item in self.results_by_topic_v3_and_below.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.results_by_topic_v3_and_below == Vec::<AddPartitionsToTxnTopicResult>::new(), "AddPartitionsToTxnResponse.ResultsByTopicV3AndBelow: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let throttle_time_ms = {
            get_i32(buf)?
        };
        let error_code = if version >= 4 {
            get_i16(buf)?
        } else {
            0i16
        };
        let results_by_transaction = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnResult::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnResult>::new()
        };
        let results_by_topic_v3_and_below = if version <= 3 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnTopicResult::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnTopicResult>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            results_by_transaction,
            results_by_topic_v3_and_below,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.throttle_time_ms = g.i32();
        if version >= 4 {
            _s.error_code = g.i16();
        }
        if version >= 4 {
            _s.results_by_transaction = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnResult::sample(version, g)).collect() };
        }
        if version <= 3 {
            _s.results_by_topic_v3_and_below = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnTopicResult::sample(version, g)).collect() };
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnTopicResult {
    /// The topic name.
    pub name: String,
    /// The results for each partition.
    pub results_by_partition: Vec<AddPartitionsToTxnPartitionResult>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnTopicResult {
    fn default() -> Self {
        Self {
            name: String::new(),
            results_by_partition: Vec::<AddPartitionsToTxnPartitionResult>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnTopicResult {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += array_len_size(self.results_by_partition.len(), _flexible);
        for _item in self.results_by_partition.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_string(buf, &self.name, _flexible)?;
        put_array_len(buf, self.results_by_partition.len(), _flexible)?;
        for _item in self.results_by_partition.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let name = {
            get_string(buf, _flexible)?
        };
        let results_by_partition = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnPartitionResult::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            results_by_partition,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnTopicResult {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.results_by_partition = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnPartitionResult::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnPartitionResult {
    /// The partition indexes.
    pub partition_index: i32,
    /// The response error code.
    pub partition_error_code: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnPartitionResult {
    fn default() -> Self {
        Self {
            partition_index: 0i32,
            partition_error_code: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnPartitionResult {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += 4;
        _n += 2;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_i32(buf, self.partition_index);
        put_i16(buf, self.partition_error_code);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let partition_index = {
            get_i32(buf)?
        };
        let partition_error_code = {
            get_i16(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            partition_index,
            partition_error_code,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnPartitionResult {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.partition_index = g.i32();
        _s.partition_error_code = g.i16();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnResult {
    /// The transactional id corresponding to the transaction.
    pub transactional_id: String,
    /// The results for each topic.
    pub topic_results: Vec<AddPartitionsToTxnTopicResult>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnResult {
    fn default() -> Self {
        Self {
            transactional_id: String::new(),
            topic_results: Vec::<AddPartitionsToTxnTopicResult>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnResult {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 4 {
            _n += string_size(&self.transactional_id, _flexible);
        }
        if version >= 4 {
            _n += array_len_size(self.topic_results.len(), _flexible);
            for _item in self.topic_results.iter() {
                _n += _item.size(version);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 4 {
            put_string(buf, &self.transactional_id, _flexible)?;
        } else {
            debug_assert!(self.transactional_id == String::new(), "AddPartitionsToTxnResponse.TransactionalId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_array_len(buf, self.topic_results.len(), _flexible)?;
            for _item in self.topic_results.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.topic_results == Vec::<AddPartitionsToTxnTopicResult>::new(), "AddPartitionsToTxnResponse.TopicResults: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let transactional_id = if version >= 4 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let topic_results = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnTopicResult::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnTopicResult>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            transactional_id,
            topic_results,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnResult {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 4 {
            _s.transactional_id = g.string();
        }
        if version >= 4 {
            _s.topic_results = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnTopicResult::sample(version, g)).collect() };
        }
        _s
    }
}

impl ApiMessage for AddPartitionsToTxnResponse {
    const API_KEY: i16 = 24;
    const NAME: &'static str = "AddPartitionsToTxnResponse";
    const VALID: VersionRange = VersionRange::new(0, 5);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

