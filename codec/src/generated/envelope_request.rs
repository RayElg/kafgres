// @generated by kafgres-codec-gen from EnvelopeRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct EnvelopeRequest {
    /// The embedded request header and data.
    pub request_data: Bytes,
    /// Value of the initial client principal when the request is redirected by a broker.
    pub request_principal: Option<Bytes>,
    /// The original client's address in bytes.
    pub client_host_address: Bytes,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for EnvelopeRequest {
    fn default() -> Self {
        Self {
            request_data: Bytes::new(),
            request_principal: Some(Bytes::new()),
            client_host_address: Bytes::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for EnvelopeRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += bytes_size(&self.request_data, _flexible);
        _n += nullable_bytes_size(self.request_principal.as_ref(), _flexible);
        _n += bytes_size(&self.client_host_address, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_bytes(buf, &self.request_data, _flexible)?;
        put_nullable_bytes(buf, self.request_principal.as_ref(), _flexible)?;
        put_bytes(buf, &self.client_host_address, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let request_data = {
            get_bytes(buf, _flexible)?
        };
        let request_principal = {
            get_nullable_bytes(buf, _flexible)?
        };
        let client_host_address = {
            get_bytes(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            request_data,
            request_principal,
            client_host_address,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for EnvelopeRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.request_data = g.bytes();
        _s.request_principal = if g.bool() { None } else { Some(g.bytes()) };
        _s.client_host_address = g.bytes();
        _s
    }
}

impl ApiMessage for EnvelopeRequest {
    const API_KEY: i16 = 58;
    const NAME: &'static str = "EnvelopeRequest";
    const VALID: VersionRange = VersionRange::new(0, 0);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

