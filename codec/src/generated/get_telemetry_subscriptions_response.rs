// @generated by kafgres-codec-gen from GetTelemetrySubscriptionsResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct GetTelemetrySubscriptionsResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The error code, or 0 if there was no error.
    pub error_code: i16,
    /// Assigned client instance id if ClientInstanceId was 0 in the request, else 0.
    pub client_instance_id: Uuid,
    /// Unique identifier for the current subscription set for this client instance.
    pub subscription_id: i32,
    /// Compression types that broker accepts for the PushTelemetryRequest.
    pub accepted_compression_types: Vec<i8>,
    /// Configured push interval, which is the lowest configured interval in the current
    /// subscription set.
    pub push_interval_ms: i32,
    /// The maximum bytes of binary data the broker accepts in PushTelemetryRequest.
    pub telemetry_max_bytes: i32,
    /// Flag to indicate monotonic/counter metrics are to be emitted as deltas or cumulative
    /// values.
    pub delta_temporality: bool,
    /// Requested metrics prefix string match. Empty array: No metrics subscribed, Array[0]
    /// empty string: All metrics subscribed.
    pub requested_metrics: Vec<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for GetTelemetrySubscriptionsResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            client_instance_id: Uuid::ZERO,
            subscription_id: 0i32,
            accepted_compression_types: Vec::<i8>::new(),
            push_interval_ms: 0i32,
            telemetry_max_bytes: 0i32,
            delta_temporality: false,
            requested_metrics: Vec::<String>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for GetTelemetrySubscriptionsResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += 2;
        _n += 16;
        _n += 4;
        _n += array_len_size(self.accepted_compression_types.len(), _flexible);
        for _item in self.accepted_compression_types.iter() {
            _n += 1;
        }
        _n += 4;
        _n += 4;
        _n += 1;
        _n += array_len_size(self.requested_metrics.len(), _flexible);
        for _item in self.requested_metrics.iter() {
            _n += string_size(&_item, _flexible);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.throttle_time_ms);
        put_i16(buf, self.error_code);
        put_uuid(buf, &self.client_instance_id);
        put_i32(buf, self.subscription_id);
        put_array_len(buf, self.accepted_compression_types.len(), _flexible)?;
        for _item in self.accepted_compression_types.iter() {
            put_i8(buf, *_item);
        }
        put_i32(buf, self.push_interval_ms);
        put_i32(buf, self.telemetry_max_bytes);
        put_bool(buf, self.delta_temporality);
        put_array_len(buf, self.requested_metrics.len(), _flexible)?;
        for _item in self.requested_metrics.iter() {
            put_string(buf, &_item, _flexible)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let throttle_time_ms = {
            get_i32(buf)?
        };
        let error_code = {
            get_i16(buf)?
        };
        let client_instance_id = {
            get_uuid(buf)?
        };
        let subscription_id = {
            get_i32(buf)?
        };
        let accepted_compression_types = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i8(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let push_interval_ms = {
            get_i32(buf)?
        };
        let telemetry_max_bytes = {
            get_i32(buf)?
        };
        let delta_temporality = {
            get_bool(buf)?
        };
        let requested_metrics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            client_instance_id,
            subscription_id,
            accepted_compression_types,
            push_interval_ms,
            telemetry_max_bytes,
            delta_temporality,
            requested_metrics,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for GetTelemetrySubscriptionsResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.throttle_time_ms = g.i32();
        _s.error_code = g.i16();
        _s.client_instance_id = g.uuid();
        _s.subscription_id = g.i32();
        _s.accepted_compression_types = { let _k = g.array_len(); (0.._k).map(|_| g.i8()).collect() };
        _s.push_interval_ms = g.i32();
        _s.telemetry_max_bytes = g.i32();
        _s.delta_temporality = g.bool();
        _s.requested_metrics = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        _s
    }
}

impl ApiMessage for GetTelemetrySubscriptionsResponse {
    const API_KEY: i16 = 71;
    const NAME: &'static str = "GetTelemetrySubscriptionsResponse";
    const VALID: VersionRange = VersionRange::new(0, 0);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

