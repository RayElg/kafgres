// @generated by kafgres-codec-gen from AddPartitionsToTxnRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnRequest {
    /// List of transactions to add partitions to.
    pub transactions: Vec<AddPartitionsToTxnTransaction>,
    /// The transactional id corresponding to the transaction.
    pub v3_and_below_transactional_id: String,
    /// Current producer id in use by the transactional id.
    pub v3_and_below_producer_id: i64,
    /// Current epoch associated with the producer id.
    pub v3_and_below_producer_epoch: i16,
    /// The partitions to add to the transaction.
    pub v3_and_below_topics: Vec<AddPartitionsToTxnTopic>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnRequest {
    fn default() -> Self {
        Self {
            transactions: Vec::<AddPartitionsToTxnTransaction>::new(),
            v3_and_below_transactional_id: String::new(),
            v3_and_below_producer_id: 0i64,
            v3_and_below_producer_epoch: 0i16,
            v3_and_below_topics: Vec::<AddPartitionsToTxnTopic>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 4 {
            _n += array_len_size(self.transactions.len(), _flexible);
            for _item in self.transactions.iter() {
                _n += _item.size(version);
            }
        }
        if version <= 3 {
            _n += string_size(&self.v3_and_below_transactional_id, _flexible);
        }
        if version <= 3 {
            _n += 8;
        }
        if version <= 3 {
            _n += 2;
        }
        if version <= 3 {
            _n += array_len_size(self.v3_and_below_topics.len(), _flexible);
            for _item in self.v3_and_below_topics.iter() {
                _n += _item.size(version);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 4 {
            put_array_len(buf, self.transactions.len(), _flexible)?;
            for _item in self.transactions.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.transactions == Vec::<AddPartitionsToTxnTransaction>::new(), "AddPartitionsToTxnRequest.Transactions: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_string(buf, &self.v3_and_below_transactional_id, _flexible)?;
        } else {
            debug_assert!(self.v3_and_below_transactional_id == String::new(), "AddPartitionsToTxnRequest.V3AndBelowTransactionalId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_i64(buf, self.v3_and_below_producer_id);
        } else {
            debug_assert!(self.v3_and_below_producer_id == 0i64, "AddPartitionsToTxnRequest.V3AndBelowProducerId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_i16(buf, self.v3_and_below_producer_epoch);
        } else {
            debug_assert!(self.v3_and_below_producer_epoch == 0i16, "AddPartitionsToTxnRequest.V3AndBelowProducerEpoch: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_array_len(buf, self.v3_and_below_topics.len(), _flexible)?;
            for _item in self.v3_and_below_topics.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.v3_and_below_topics == Vec::<AddPartitionsToTxnTopic>::new(), "AddPartitionsToTxnRequest.V3AndBelowTopics: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let transactions = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnTransaction::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnTransaction>::new()
        };
        let v3_and_below_transactional_id = if version <= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let v3_and_below_producer_id = if version <= 3 {
            get_i64(buf)?
        } else {
            0i64
        };
        let v3_and_below_producer_epoch = if version <= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let v3_and_below_topics = if version <= 3 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnTopic::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnTopic>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            transactions,
            v3_and_below_transactional_id,
            v3_and_below_producer_id,
            v3_and_below_producer_epoch,
            v3_and_below_topics,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 4 {
            _s.transactions = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnTransaction::sample(version, g)).collect() };
        }
        if version <= 3 {
            _s.v3_and_below_transactional_id = g.string();
        }
        if version <= 3 {
            _s.v3_and_below_producer_id = g.i64();
        }
        if version <= 3 {
            _s.v3_and_below_producer_epoch = g.i16();
        }
        if version <= 3 {
            _s.v3_and_below_topics = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnTopic::sample(version, g)).collect() };
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnTopic {
    /// The name of the topic.
    pub name: String,
    /// The partition indexes to add to the transaction.
    pub partitions: Vec<i32>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnTopic {
    fn default() -> Self {
        Self {
            name: String::new(),
            partitions: Vec::<i32>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnTopic {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += array_len_size(self.partitions.len(), _flexible);
        for _item in self.partitions.iter() {
            _n += 4;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        put_string(buf, &self.name, _flexible)?;
        put_array_len(buf, self.partitions.len(), _flexible)?;
        for _item in self.partitions.iter() {
            put_i32(buf, *_item);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let name = {
            get_string(buf, _flexible)?
        };
        let partitions = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i32(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            partitions,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnTopic {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.partitions = { let _k = g.array_len(); (0.._k).map(|_| g.i32()).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AddPartitionsToTxnTransaction {
    /// The transactional id corresponding to the transaction.
    pub transactional_id: String,
    /// Current producer id in use by the transactional id.
    pub producer_id: i64,
    /// Current epoch associated with the producer id.
    pub producer_epoch: i16,
    /// Boolean to signify if we want to check if the partition is in the transaction rather
    /// than add it.
    pub verify_only: bool,
    /// The partitions to add to the transaction.
    pub topics: Vec<AddPartitionsToTxnTopic>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AddPartitionsToTxnTransaction {
    fn default() -> Self {
        Self {
            transactional_id: String::new(),
            producer_id: 0i64,
            producer_epoch: 0i16,
            verify_only: false,
            topics: Vec::<AddPartitionsToTxnTopic>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AddPartitionsToTxnTransaction {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 4 {
            _n += string_size(&self.transactional_id, _flexible);
        }
        if version >= 4 {
            _n += 8;
        }
        if version >= 4 {
            _n += 2;
        }
        if version >= 4 {
            _n += 1;
        }
        if version >= 4 {
            _n += array_len_size(self.topics.len(), _flexible);
            for _item in self.topics.iter() {
                _n += _item.size(version);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 4 {
            put_string(buf, &self.transactional_id, _flexible)?;
        } else {
            debug_assert!(self.transactional_id == String::new(), "AddPartitionsToTxnRequest.TransactionalId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_i64(buf, self.producer_id);
        } else {
            debug_assert!(self.producer_id == 0i64, "AddPartitionsToTxnRequest.ProducerId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_i16(buf, self.producer_epoch);
        } else {
            debug_assert!(self.producer_epoch == 0i16, "AddPartitionsToTxnRequest.ProducerEpoch: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_bool(buf, self.verify_only);
        } else {
            debug_assert!(self.verify_only == false, "AddPartitionsToTxnRequest.VerifyOnly: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_array_len(buf, self.topics.len(), _flexible)?;
            for _item in self.topics.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.topics == Vec::<AddPartitionsToTxnTopic>::new(), "AddPartitionsToTxnRequest.Topics: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let transactional_id = if version >= 4 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let producer_id = if version >= 4 {
            get_i64(buf)?
        } else {
            0i64
        };
        let producer_epoch = if version >= 4 {
            get_i16(buf)?
        } else {
            0i16
        };
        let verify_only = if version >= 4 {
            get_bool(buf)?
        } else {
            false
        };
        let topics = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AddPartitionsToTxnTopic::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<AddPartitionsToTxnTopic>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            transactional_id,
            producer_id,
            producer_epoch,
            verify_only,
            topics,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AddPartitionsToTxnTransaction {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 4 {
            _s.transactional_id = g.string();
        }
        if version >= 4 {
            _s.producer_id = g.i64();
        }
        if version >= 4 {
            _s.producer_epoch = g.i16();
        }
        if version >= 4 {
            _s.verify_only = g.bool();
        }
        if version >= 4 {
            _s.topics = { let _k = g.array_len(); (0.._k).map(|_| AddPartitionsToTxnTopic::sample(version, g)).collect() };
        }
        _s
    }
}

impl ApiMessage for AddPartitionsToTxnRequest {
    const API_KEY: i16 = 24;
    const NAME: &'static str = "AddPartitionsToTxnRequest";
    const VALID: VersionRange = VersionRange::new(0, 5);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

