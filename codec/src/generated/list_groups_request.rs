// @generated by kafgres-codec-gen from ListGroupsRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct ListGroupsRequest {
    /// The states of the groups we want to list. If empty, all groups are returned with
    /// their state.
    pub states_filter: Vec<String>,
    /// The types of the groups we want to list. If empty, all groups are returned with
    /// their type.
    pub types_filter: Vec<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for ListGroupsRequest {
    fn default() -> Self {
        Self {
            states_filter: Vec::<String>::new(),
            types_filter: Vec::<String>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for ListGroupsRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 4 {
            _n += array_len_size(self.states_filter.len(), _flexible);
            for _item in self.states_filter.iter() {
                _n += string_size(&_item, _flexible);
            }
        }
        if version >= 5 {
            _n += array_len_size(self.types_filter.len(), _flexible);
            for _item in self.types_filter.iter() {
                _n += string_size(&_item, _flexible);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 4 {
            put_array_len(buf, self.states_filter.len(), _flexible)?;
            for _item in self.states_filter.iter() {
                put_string(buf, &_item, _flexible)?;
            }
        } else {
            debug_assert!(self.states_filter == Vec::<String>::new(), "ListGroupsRequest.StatesFilter: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 5 {
            put_array_len(buf, self.types_filter.len(), _flexible)?;
            for _item in self.types_filter.iter() {
                put_string(buf, &_item, _flexible)?;
            }
        } else {
            debug_assert!(self.types_filter == Vec::<String>::new(), "ListGroupsRequest.TypesFilter: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let states_filter = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<String>::new()
        };
        let types_filter = if version >= 5 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<String>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            states_filter,
            types_filter,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for ListGroupsRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 4 {
            _s.states_filter = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        }
        if version >= 5 {
            _s.types_filter = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        }
        _s
    }
}

impl ApiMessage for ListGroupsRequest {
    const API_KEY: i16 = 16;
    const NAME: &'static str = "ListGroupsRequest";
    const VALID: VersionRange = VersionRange::new(0, 5);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

