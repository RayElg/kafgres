// @generated by kafgres-codec-gen from CreateDelegationTokenRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct CreateDelegationTokenRequest {
    /// The principal type of the owner of the token. If it's null it defaults to the token
    /// request principal.
    pub owner_principal_type: Option<String>,
    /// The principal name of the owner of the token. If it's null it defaults to the token
    /// request principal.
    pub owner_principal_name: Option<String>,
    /// A list of those who are allowed to renew this token before it expires.
    pub renewers: Vec<CreatableRenewers>,
    /// The maximum lifetime of the token in milliseconds, or -1 to use the server side
    /// default.
    pub max_lifetime_ms: i64,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for CreateDelegationTokenRequest {
    fn default() -> Self {
        Self {
            owner_principal_type: Some(String::new()),
            owner_principal_name: Some(String::new()),
            renewers: Vec::<CreatableRenewers>::new(),
            max_lifetime_ms: 0i64,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for CreateDelegationTokenRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        if version >= 3 {
            if version >= 3 {
                _n += nullable_string_size(self.owner_principal_type.as_deref(), _flexible);
            } else if let Some(_v) = &self.owner_principal_type {
                _n += string_size(&_v, _flexible);
            }
        }
        if version >= 3 {
            if version >= 3 {
                _n += nullable_string_size(self.owner_principal_name.as_deref(), _flexible);
            } else if let Some(_v) = &self.owner_principal_name {
                _n += string_size(&_v, _flexible);
            }
        }
        _n += array_len_size(self.renewers.len(), _flexible);
        for _item in self.renewers.iter() {
            _n += _item.size(version);
        }
        _n += 8;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        if version >= 3 {
            if version >= 3 {
                put_nullable_string(buf, self.owner_principal_type.as_deref(), _flexible)?;
            } else {
                let _v = self.owner_principal_type.as_ref().ok_or(CodecError::UnexpectedNull("OwnerPrincipalType"))?;
                put_string(buf, &_v, _flexible)?;
            }
        } else {
            debug_assert!(self.owner_principal_type == Some(String::new()), "CreateDelegationTokenRequest.OwnerPrincipalType: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 3 {
            if version >= 3 {
                put_nullable_string(buf, self.owner_principal_name.as_deref(), _flexible)?;
            } else {
                let _v = self.owner_principal_name.as_ref().ok_or(CodecError::UnexpectedNull("OwnerPrincipalName"))?;
                put_string(buf, &_v, _flexible)?;
            }
        } else {
            debug_assert!(self.owner_principal_name == Some(String::new()), "CreateDelegationTokenRequest.OwnerPrincipalName: non-ignorable field set at version {} that cannot carry it", version);
        }
        put_array_len(buf, self.renewers.len(), _flexible)?;
        for _item in self.renewers.iter() {
            _item.encode(buf, version)?;
        }
        put_i64(buf, self.max_lifetime_ms);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let owner_principal_type = if version >= 3 {
            if version >= 3 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            Some(String::new())
        };
        let owner_principal_name = if version >= 3 {
            if version >= 3 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            Some(String::new())
        };
        let renewers = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            CreatableRenewers::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let max_lifetime_ms = {
            get_i64(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            owner_principal_type,
            owner_principal_name,
            renewers,
            max_lifetime_ms,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for CreateDelegationTokenRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        if version >= 3 {
            _s.owner_principal_type = if version >= 3 && g.bool() { None } else { Some(g.string()) };
        }
        if version >= 3 {
            _s.owner_principal_name = if version >= 3 && g.bool() { None } else { Some(g.string()) };
        }
        _s.renewers = { let _k = g.array_len(); (0.._k).map(|_| CreatableRenewers::sample(version, g)).collect() };
        _s.max_lifetime_ms = g.i64();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct CreatableRenewers {
    /// The type of the Kafka principal.
    pub principal_type: String,
    /// The name of the Kafka principal.
    pub principal_name: String,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for CreatableRenewers {
    fn default() -> Self {
        Self {
            principal_type: String::new(),
            principal_name: String::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for CreatableRenewers {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 2;
        let mut _n = 0usize;
        _n += string_size(&self.principal_type, _flexible);
        _n += string_size(&self.principal_name, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 2;
        put_string(buf, &self.principal_type, _flexible)?;
        put_string(buf, &self.principal_name, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 2;
        let principal_type = {
            get_string(buf, _flexible)?
        };
        let principal_name = {
            get_string(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            principal_type,
            principal_name,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for CreatableRenewers {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 2;
        let mut _s = Self::default();
        _s.principal_type = g.string();
        _s.principal_name = g.string();
        _s
    }
}

impl ApiMessage for CreateDelegationTokenRequest {
    const API_KEY: i16 = 38;
    const NAME: &'static str = "CreateDelegationTokenRequest";
    const VALID: VersionRange = VersionRange::new(1, 3);
    const FLEXIBLE: VersionRange = VersionRange::new(2, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

