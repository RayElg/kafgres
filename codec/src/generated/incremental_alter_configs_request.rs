// @generated by kafgres-codec-gen from IncrementalAlterConfigsRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct IncrementalAlterConfigsRequest {
    /// The incremental updates for each resource.
    pub resources: Vec<AlterConfigsResource>,
    /// True if we should validate the request, but not change the configurations.
    pub validate_only: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for IncrementalAlterConfigsRequest {
    fn default() -> Self {
        Self {
            resources: Vec::<AlterConfigsResource>::new(),
            validate_only: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for IncrementalAlterConfigsRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += array_len_size(self.resources.len(), _flexible);
        for _item in self.resources.iter() {
            _n += _item.size(version);
        }
        _n += 1;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_array_len(buf, self.resources.len(), _flexible)?;
        for _item in self.resources.iter() {
            _item.encode(buf, version)?;
        }
        put_bool(buf, self.validate_only);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let resources = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AlterConfigsResource::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let validate_only = {
            get_bool(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            resources,
            validate_only,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for IncrementalAlterConfigsRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.resources = { let _k = g.array_len(); (0.._k).map(|_| AlterConfigsResource::sample(version, g)).collect() };
        _s.validate_only = g.bool();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AlterConfigsResource {
    /// The resource type.
    pub resource_type: i8,
    /// The resource name.
    pub resource_name: String,
    /// The configurations.
    pub configs: Vec<AlterableConfig>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AlterConfigsResource {
    fn default() -> Self {
        Self {
            resource_type: 0i8,
            resource_name: String::new(),
            configs: Vec::<AlterableConfig>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AlterConfigsResource {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += 1;
        _n += string_size(&self.resource_name, _flexible);
        _n += array_len_size(self.configs.len(), _flexible);
        for _item in self.configs.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_i8(buf, self.resource_type);
        put_string(buf, &self.resource_name, _flexible)?;
        put_array_len(buf, self.configs.len(), _flexible)?;
        for _item in self.configs.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let resource_type = {
            get_i8(buf)?
        };
        let resource_name = {
            get_string(buf, _flexible)?
        };
        let configs = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            AlterableConfig::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            resource_type,
            resource_name,
            configs,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AlterConfigsResource {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.resource_type = g.i8();
        _s.resource_name = g.string();
        _s.configs = { let _k = g.array_len(); (0.._k).map(|_| AlterableConfig::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct AlterableConfig {
    /// The configuration key name.
    pub name: String,
    /// The type (Set, Delete, Append, Subtract) of operation.
    pub config_operation: i8,
    /// The value to set for the configuration key.
    pub value: Option<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for AlterableConfig {
    fn default() -> Self {
        Self {
            name: String::new(),
            config_operation: 0i8,
            value: Some(String::new()),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for AlterableConfig {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += 1;
        _n += nullable_string_size(self.value.as_deref(), _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_string(buf, &self.name, _flexible)?;
        put_i8(buf, self.config_operation);
        put_nullable_string(buf, self.value.as_deref(), _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let name = {
            get_string(buf, _flexible)?
        };
        let config_operation = {
            get_i8(buf)?
        };
        let value = {
            get_nullable_string(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            config_operation,
            value,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for AlterableConfig {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.config_operation = g.i8();
        _s.value = if g.bool() { None } else { Some(g.string()) };
        _s
    }
}

impl ApiMessage for IncrementalAlterConfigsRequest {
    const API_KEY: i16 = 44;
    const NAME: &'static str = "IncrementalAlterConfigsRequest";
    const VALID: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE: VersionRange = VersionRange::new(1, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

