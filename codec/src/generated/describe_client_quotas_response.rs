// @generated by kafgres-codec-gen from DescribeClientQuotasResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct DescribeClientQuotasResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The error code, or `0` if the quota description succeeded.
    pub error_code: i16,
    /// The error message, or `null` if the quota description succeeded.
    pub error_message: Option<String>,
    /// A result entry.
    pub entries: Option<Vec<EntryData>>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DescribeClientQuotasResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            error_message: Some(String::new()),
            entries: Some(Vec::<EntryData>::new()),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DescribeClientQuotasResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += 4;
        _n += 2;
        _n += nullable_string_size(self.error_message.as_deref(), _flexible);
        match &self.entries {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_i32(buf, self.throttle_time_ms);
        put_i16(buf, self.error_code);
        put_nullable_string(buf, self.error_message.as_deref(), _flexible)?;
        match &self.entries {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let throttle_time_ms = {
            get_i32(buf)?
        };
        let error_code = {
            get_i16(buf)?
        };
        let error_message = {
            get_nullable_string(buf, _flexible)?
        };
        let entries = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            EntryData::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            error_message,
            entries,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DescribeClientQuotasResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.throttle_time_ms = g.i32();
        _s.error_code = g.i16();
        _s.error_message = if g.bool() { None } else { Some(g.string()) };
        _s.entries = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| EntryData::sample(version, g)).collect() }) };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct EntryData {
    /// The quota entity description.
    pub entity: Vec<EntityData>,
    /// The quota values for the entity.
    pub values: Vec<ValueData>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for EntryData {
    fn default() -> Self {
        Self {
            entity: Vec::<EntityData>::new(),
            values: Vec::<ValueData>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for EntryData {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += array_len_size(self.entity.len(), _flexible);
        for _item in self.entity.iter() {
            _n += _item.size(version);
        }
        _n += array_len_size(self.values.len(), _flexible);
        for _item in self.values.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_array_len(buf, self.entity.len(), _flexible)?;
        for _item in self.entity.iter() {
            _item.encode(buf, version)?;
        }
        put_array_len(buf, self.values.len(), _flexible)?;
        for _item in self.values.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let entity = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            EntityData::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let values = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            ValueData::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            entity,
            values,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for EntryData {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.entity = { let _k = g.array_len(); (0.._k).map(|_| EntityData::sample(version, g)).collect() };
        _s.values = { let _k = g.array_len(); (0.._k).map(|_| ValueData::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct EntityData {
    /// The entity type.
    pub entity_type: String,
    /// The entity name, or null if the default.
    pub entity_name: Option<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for EntityData {
    fn default() -> Self {
        Self {
            entity_type: String::new(),
            entity_name: Some(String::new()),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for EntityData {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += string_size(&self.entity_type, _flexible);
        _n += nullable_string_size(self.entity_name.as_deref(), _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_string(buf, &self.entity_type, _flexible)?;
        put_nullable_string(buf, self.entity_name.as_deref(), _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let entity_type = {
            get_string(buf, _flexible)?
        };
        let entity_name = {
            get_nullable_string(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            entity_type,
            entity_name,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for EntityData {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.entity_type = g.string();
        _s.entity_name = if g.bool() { None } else { Some(g.string()) };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct ValueData {
    /// The quota configuration key.
    pub key: String,
    /// The quota configuration value.
    pub value: f64,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for ValueData {
    fn default() -> Self {
        Self {
            key: String::new(),
            value: 0.0,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for ValueData {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 1;
        let mut _n = 0usize;
        _n += string_size(&self.key, _flexible);
        _n += 8;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 1;
        put_string(buf, &self.key, _flexible)?;
        put_f64(buf, self.value);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 1;
        let key = {
            get_string(buf, _flexible)?
        };
        let value = {
            get_f64(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            key,
            value,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for ValueData {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 1;
        let mut _s = Self::default();
        _s.key = g.string();
        _s.value = g.f64();
        _s
    }
}

impl ApiMessage for DescribeClientQuotasResponse {
    const API_KEY: i16 = 48;
    const NAME: &'static str = "DescribeClientQuotasResponse";
    const VALID: VersionRange = VersionRange::new(0, 1);
    const FLEXIBLE: VersionRange = VersionRange::new(1, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

