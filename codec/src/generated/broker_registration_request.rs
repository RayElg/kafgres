// @generated by kafgres-codec-gen from BrokerRegistrationRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct BrokerRegistrationRequest {
    /// The broker ID.
    pub broker_id: i32,
    /// The cluster id of the broker process.
    pub cluster_id: String,
    /// The incarnation id of the broker process.
    pub incarnation_id: Uuid,
    /// The listeners of this broker.
    pub listeners: Vec<Listener>,
    /// The features on this broker. Note: in v0-v3, features with MinSupportedVersion = 0
    /// are omitted.
    pub features: Vec<Feature>,
    /// The rack which this broker is in.
    pub rack: Option<String>,
    /// If the required configurations for ZK migration are present, this value is set to
    /// true.
    pub is_migrating_zk_broker: bool,
    /// Log directories configured in this broker which are available.
    pub log_dirs: Vec<Uuid>,
    /// The epoch before a clean shutdown.
    pub previous_broker_epoch: i64,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for BrokerRegistrationRequest {
    fn default() -> Self {
        Self {
            broker_id: 0i32,
            cluster_id: String::new(),
            incarnation_id: Uuid::ZERO,
            listeners: Vec::<Listener>::new(),
            features: Vec::<Feature>::new(),
            rack: Some(String::new()),
            is_migrating_zk_broker: false,
            log_dirs: Vec::<Uuid>::new(),
            previous_broker_epoch: -1i64,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for BrokerRegistrationRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += string_size(&self.cluster_id, _flexible);
        _n += 16;
        _n += array_len_size(self.listeners.len(), _flexible);
        for _item in self.listeners.iter() {
            _n += _item.size(version);
        }
        _n += array_len_size(self.features.len(), _flexible);
        for _item in self.features.iter() {
            _n += _item.size(version);
        }
        _n += nullable_string_size(self.rack.as_deref(), _flexible);
        if version >= 1 {
            _n += 1;
        }
        if version >= 2 {
            _n += array_len_size(self.log_dirs.len(), _flexible);
            for _item in self.log_dirs.iter() {
                _n += 16;
            }
        }
        if version >= 3 {
            _n += 8;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.broker_id);
        put_string(buf, &self.cluster_id, _flexible)?;
        put_uuid(buf, &self.incarnation_id);
        put_array_len(buf, self.listeners.len(), _flexible)?;
        for _item in self.listeners.iter() {
            _item.encode(buf, version)?;
        }
        put_array_len(buf, self.features.len(), _flexible)?;
        for _item in self.features.iter() {
            _item.encode(buf, version)?;
        }
        put_nullable_string(buf, self.rack.as_deref(), _flexible)?;
        if version >= 1 {
            put_bool(buf, self.is_migrating_zk_broker);
        } else {
            debug_assert!(self.is_migrating_zk_broker == false, "BrokerRegistrationRequest.IsMigratingZkBroker: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 2 {
            put_array_len(buf, self.log_dirs.len(), _flexible)?;
            for _item in self.log_dirs.iter() {
                put_uuid(buf, &_item);
            }
        }
        if version >= 3 {
            put_i64(buf, self.previous_broker_epoch);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let broker_id = {
            get_i32(buf)?
        };
        let cluster_id = {
            get_string(buf, _flexible)?
        };
        let incarnation_id = {
            get_uuid(buf)?
        };
        let listeners = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            Listener::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let features = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            Feature::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let rack = {
            get_nullable_string(buf, _flexible)?
        };
        let is_migrating_zk_broker = if version >= 1 {
            get_bool(buf)?
        } else {
            false
        };
        let log_dirs = if version >= 2 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_uuid(buf)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<Uuid>::new()
        };
        let previous_broker_epoch = if version >= 3 {
            get_i64(buf)?
        } else {
            -1i64
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            broker_id,
            cluster_id,
            incarnation_id,
            listeners,
            features,
            rack,
            is_migrating_zk_broker,
            log_dirs,
            previous_broker_epoch,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for BrokerRegistrationRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.broker_id = g.i32();
        _s.cluster_id = g.string();
        _s.incarnation_id = g.uuid();
        _s.listeners = { let _k = g.array_len(); (0.._k).map(|_| Listener::sample(version, g)).collect() };
        _s.features = { let _k = g.array_len(); (0.._k).map(|_| Feature::sample(version, g)).collect() };
        _s.rack = if g.bool() { None } else { Some(g.string()) };
        if version >= 1 {
            _s.is_migrating_zk_broker = g.bool();
        }
        if version >= 2 {
            _s.log_dirs = { let _k = g.array_len(); (0.._k).map(|_| g.uuid()).collect() };
        }
        if version >= 3 {
            _s.previous_broker_epoch = g.i64();
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Listener {
    /// The name of the endpoint.
    pub name: String,
    /// The hostname.
    pub host: String,
    /// The port.
    pub port: u16,
    /// The security protocol.
    pub security_protocol: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Listener {
    fn default() -> Self {
        Self {
            name: String::new(),
            host: String::new(),
            port: 0u16,
            security_protocol: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Listener {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += string_size(&self.host, _flexible);
        _n += 2;
        _n += 2;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.name, _flexible)?;
        put_string(buf, &self.host, _flexible)?;
        put_u16(buf, self.port);
        put_i16(buf, self.security_protocol);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let name = {
            get_string(buf, _flexible)?
        };
        let host = {
            get_string(buf, _flexible)?
        };
        let port = {
            get_u16(buf)?
        };
        let security_protocol = {
            get_i16(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            host,
            port,
            security_protocol,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Listener {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.host = g.string();
        _s.port = g.u16();
        _s.security_protocol = g.i16();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Feature {
    /// The feature name.
    pub name: String,
    /// The minimum supported feature level.
    pub min_supported_version: i16,
    /// The maximum supported feature level.
    pub max_supported_version: i16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Feature {
    fn default() -> Self {
        Self {
            name: String::new(),
            min_supported_version: 0i16,
            max_supported_version: 0i16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Feature {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += 2;
        _n += 2;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.name, _flexible)?;
        put_i16(buf, self.min_supported_version);
        put_i16(buf, self.max_supported_version);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let name = {
            get_string(buf, _flexible)?
        };
        let min_supported_version = {
            get_i16(buf)?
        };
        let max_supported_version = {
            get_i16(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            min_supported_version,
            max_supported_version,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Feature {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.min_supported_version = g.i16();
        _s.max_supported_version = g.i16();
        _s
    }
}

impl ApiMessage for BrokerRegistrationRequest {
    const API_KEY: i16 = 62;
    const NAME: &'static str = "BrokerRegistrationRequest";
    const VALID: VersionRange = VersionRange::new(0, 4);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

