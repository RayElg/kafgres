// @generated by kafgres-codec-gen from DeleteTopicsRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct DeleteTopicsRequest {
    /// The name or topic ID of the topic.
    pub topics: Vec<DeleteTopicState>,
    /// The names of the topics to delete.
    pub topic_names: Vec<String>,
    /// The length of time in milliseconds to wait for the deletions to complete.
    pub timeout_ms: i32,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DeleteTopicsRequest {
    fn default() -> Self {
        Self {
            topics: Vec::<DeleteTopicState>::new(),
            topic_names: Vec::<String>::new(),
            timeout_ms: 0i32,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DeleteTopicsRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 4;
        let mut _n = 0usize;
        if version >= 6 {
            _n += array_len_size(self.topics.len(), _flexible);
            for _item in self.topics.iter() {
                _n += _item.size(version);
            }
        }
        if version <= 5 {
            _n += array_len_size(self.topic_names.len(), _flexible);
            for _item in self.topic_names.iter() {
                _n += string_size(&_item, _flexible);
            }
        }
        _n += 4;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 4;
        if version >= 6 {
            put_array_len(buf, self.topics.len(), _flexible)?;
            for _item in self.topics.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.topics == Vec::<DeleteTopicState>::new(), "DeleteTopicsRequest.Topics: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 5 {
            put_array_len(buf, self.topic_names.len(), _flexible)?;
            for _item in self.topic_names.iter() {
                put_string(buf, &_item, _flexible)?;
            }
        }
        put_i32(buf, self.timeout_ms);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 4;
        let topics = if version >= 6 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            DeleteTopicState::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<DeleteTopicState>::new()
        };
        let topic_names = if version <= 5 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<String>::new()
        };
        let timeout_ms = {
            get_i32(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            topics,
            topic_names,
            timeout_ms,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DeleteTopicsRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 4;
        let mut _s = Self::default();
        if version >= 6 {
            _s.topics = { let _k = g.array_len(); (0.._k).map(|_| DeleteTopicState::sample(version, g)).collect() };
        }
        if version <= 5 {
            _s.topic_names = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        }
        _s.timeout_ms = g.i32();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DeleteTopicState {
    /// The topic name.
    pub name: Option<String>,
    /// The unique topic ID.
    pub topic_id: Uuid,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for DeleteTopicState {
    fn default() -> Self {
        Self {
            name: None,
            topic_id: Uuid::ZERO,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for DeleteTopicState {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 4;
        let mut _n = 0usize;
        if version >= 6 {
            if version >= 6 {
                _n += nullable_string_size(self.name.as_deref(), _flexible);
            } else if let Some(_v) = &self.name {
                _n += string_size(&_v, _flexible);
            }
        }
        if version >= 6 {
            _n += 16;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 4;
        if version >= 6 {
            if version >= 6 {
                put_nullable_string(buf, self.name.as_deref(), _flexible)?;
            } else {
                let _v = self.name.as_ref().ok_or(CodecError::UnexpectedNull("Name"))?;
                put_string(buf, &_v, _flexible)?;
            }
        } else {
            debug_assert!(self.name == None, "DeleteTopicsRequest.Name: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 6 {
            put_uuid(buf, &self.topic_id);
        } else {
            debug_assert!(self.topic_id == Uuid::ZERO, "DeleteTopicsRequest.TopicId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 4;
        let name = if version >= 6 {
            if version >= 6 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            None
        };
        let topic_id = if version >= 6 {
            get_uuid(buf)?
        } else {
            Uuid::ZERO
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            topic_id,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for DeleteTopicState {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 4;
        let mut _s = Self::default();
        if version >= 6 {
            _s.name = if version >= 6 && g.bool() { None } else { Some(g.string()) };
        }
        if version >= 6 {
            _s.topic_id = g.uuid();
        }
        _s
    }
}

impl ApiMessage for DeleteTopicsRequest {
    const API_KEY: i16 = 20;
    const NAME: &'static str = "DeleteTopicsRequest";
    const VALID: VersionRange = VersionRange::new(1, 6);
    const FLEXIBLE: VersionRange = VersionRange::new(4, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

