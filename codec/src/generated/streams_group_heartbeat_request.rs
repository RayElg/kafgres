// @generated by kafgres-codec-gen from StreamsGroupHeartbeatRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct StreamsGroupHeartbeatRequest {
    /// The group identifier.
    pub group_id: String,
    /// The member ID generated by the streams consumer. The member ID must be kept during
    /// the entire lifetime of the streams consumer process.
    pub member_id: String,
    /// The current member epoch; 0 to join the group; -1 to leave the group; -2 to indicate
    /// that the static member will rejoin.
    pub member_epoch: i32,
    /// The current endpoint epoch of this client, represents the latest endpoint epoch this
    /// client received
    pub endpoint_information_epoch: i32,
    /// null if not provided or if it didn't change since the last heartbeat; the instance
    /// ID for static membership otherwise.
    pub instance_id: Option<String>,
    /// null if not provided or if it didn't change since the last heartbeat; the rack ID of
    /// the member otherwise.
    pub rack_id: Option<String>,
    /// -1 if it didn't change since the last heartbeat; the maximum time in milliseconds
    /// that the coordinator will wait on the member to revoke its tasks otherwise.
    pub rebalance_timeout_ms: i32,
    /// The topology metadata of the streams application. Used to initialize the topology of
    /// the group and to check if the topology corresponds to the topology initialized for
    /// the group. Only sent when memberEpoch = 0, must be non-empty. Null otherwise.
    pub topology: Option<Topology>,
    /// Currently owned active tasks for this client. Null if unchanged since last
    /// heartbeat.
    pub active_tasks: Option<Vec<TaskIds>>,
    /// Currently owned standby tasks for this client. Null if unchanged since last
    /// heartbeat.
    pub standby_tasks: Option<Vec<TaskIds>>,
    /// Currently owned warm-up tasks for this client. Null if unchanged since last
    /// heartbeat.
    pub warmup_tasks: Option<Vec<TaskIds>>,
    /// Identity of the streams instance that may have multiple consumers. Null if unchanged
    /// since last heartbeat.
    pub process_id: Option<String>,
    /// User-defined endpoint for Interactive Queries. Null if unchanged since last
    /// heartbeat, or if not defined on the client.
    pub user_endpoint: Option<Endpoint>,
    /// Used for rack-aware assignment algorithm. Null if unchanged since last heartbeat.
    pub client_tags: Option<Vec<KeyValue>>,
    /// Cumulative changelog offsets for tasks. Only updated when a warm-up task has caught
    /// up, and according to the task offset interval. Null if unchanged since last
    /// heartbeat.
    pub task_offsets: Option<Vec<TaskOffset>>,
    /// Cumulative changelog end-offsets for tasks. Only updated when a warm-up task has
    /// caught up, and according to the task offset interval. Null if unchanged since last
    /// heartbeat.
    pub task_end_offsets: Option<Vec<TaskOffset>>,
    /// Whether all Streams clients in the group should shut down.
    pub shutdown_application: bool,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for StreamsGroupHeartbeatRequest {
    fn default() -> Self {
        Self {
            group_id: String::new(),
            member_id: String::new(),
            member_epoch: 0i32,
            endpoint_information_epoch: 0i32,
            instance_id: None,
            rack_id: None,
            rebalance_timeout_ms: -1i32,
            topology: None,
            active_tasks: None,
            standby_tasks: None,
            warmup_tasks: None,
            process_id: None,
            user_endpoint: None,
            client_tags: None,
            task_offsets: None,
            task_end_offsets: None,
            shutdown_application: false,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for StreamsGroupHeartbeatRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.group_id, _flexible);
        _n += string_size(&self.member_id, _flexible);
        _n += 4;
        _n += 4;
        _n += nullable_string_size(self.instance_id.as_deref(), _flexible);
        _n += nullable_string_size(self.rack_id.as_deref(), _flexible);
        _n += 4;
        match &self.topology {
            None => _n += 1,
            Some(_v) => {
                _n += 1;
                _n += _v.size(version);
            }
        }
        match &self.active_tasks {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        match &self.standby_tasks {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        match &self.warmup_tasks {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        _n += nullable_string_size(self.process_id.as_deref(), _flexible);
        match &self.user_endpoint {
            None => _n += 1,
            Some(_v) => {
                _n += 1;
                _n += _v.size(version);
            }
        }
        match &self.client_tags {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        match &self.task_offsets {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        match &self.task_end_offsets {
            None => _n += null_array_size(_flexible),
            Some(_items) => {
                _n += array_len_size(_items.len(), _flexible);
                for _item in _items.iter() {
                    _n += _item.size(version);
                }
            }
        }
        _n += 1;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.group_id, _flexible)?;
        put_string(buf, &self.member_id, _flexible)?;
        put_i32(buf, self.member_epoch);
        put_i32(buf, self.endpoint_information_epoch);
        put_nullable_string(buf, self.instance_id.as_deref(), _flexible)?;
        put_nullable_string(buf, self.rack_id.as_deref(), _flexible)?;
        put_i32(buf, self.rebalance_timeout_ms);
        match &self.topology {
            None => put_i8(buf, -1),
            Some(_v) => {
                put_i8(buf, 1);
                _v.encode(buf, version)?;
            }
        }
        match &self.active_tasks {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        match &self.standby_tasks {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        match &self.warmup_tasks {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        put_nullable_string(buf, self.process_id.as_deref(), _flexible)?;
        match &self.user_endpoint {
            None => put_i8(buf, -1),
            Some(_v) => {
                put_i8(buf, 1);
                _v.encode(buf, version)?;
            }
        }
        match &self.client_tags {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        match &self.task_offsets {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        match &self.task_end_offsets {
            None => put_null_array(buf, _flexible),
            Some(_items) => {
                put_array_len(buf, _items.len(), _flexible)?;
                for _item in _items.iter() {
                    _item.encode(buf, version)?;
                }
            }
        }
        put_bool(buf, self.shutdown_application);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let group_id = {
            get_string(buf, _flexible)?
        };
        let member_id = {
            get_string(buf, _flexible)?
        };
        let member_epoch = {
            get_i32(buf)?
        };
        let endpoint_information_epoch = {
            get_i32(buf)?
        };
        let instance_id = {
            get_nullable_string(buf, _flexible)?
        };
        let rack_id = {
            get_nullable_string(buf, _flexible)?
        };
        let rebalance_timeout_ms = {
            get_i32(buf)?
        };
        let topology = {
            if get_i8(buf)? < 0 {
                None
            } else {
                Some(Topology::decode(buf, version)?)
            }
        };
        let active_tasks = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TaskIds::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let standby_tasks = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TaskIds::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let warmup_tasks = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TaskIds::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let process_id = {
            get_nullable_string(buf, _flexible)?
        };
        let user_endpoint = {
            if get_i8(buf)? < 0 {
                None
            } else {
                Some(Endpoint::decode(buf, version)?)
            }
        };
        let client_tags = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            KeyValue::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let task_offsets = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TaskOffset::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let task_end_offsets = {
            match get_array_len(buf, _flexible)? {
                None => None,
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TaskOffset::decode(buf, version)?
                        });
                    }
                    Some(_v)
                }
            }
        };
        let shutdown_application = {
            get_bool(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            group_id,
            member_id,
            member_epoch,
            endpoint_information_epoch,
            instance_id,
            rack_id,
            rebalance_timeout_ms,
            topology,
            active_tasks,
            standby_tasks,
            warmup_tasks,
            process_id,
            user_endpoint,
            client_tags,
            task_offsets,
            task_end_offsets,
            shutdown_application,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for StreamsGroupHeartbeatRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.group_id = g.string();
        _s.member_id = g.string();
        _s.member_epoch = g.i32();
        _s.endpoint_information_epoch = g.i32();
        _s.instance_id = if g.bool() { None } else { Some(g.string()) };
        _s.rack_id = if g.bool() { None } else { Some(g.string()) };
        _s.rebalance_timeout_ms = g.i32();
        _s.topology = if g.bool() { None } else { Some(Topology::sample(version, g)) };
        _s.active_tasks = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| TaskIds::sample(version, g)).collect() }) };
        _s.standby_tasks = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| TaskIds::sample(version, g)).collect() }) };
        _s.warmup_tasks = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| TaskIds::sample(version, g)).collect() }) };
        _s.process_id = if g.bool() { None } else { Some(g.string()) };
        _s.user_endpoint = if g.bool() { None } else { Some(Endpoint::sample(version, g)) };
        _s.client_tags = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| KeyValue::sample(version, g)).collect() }) };
        _s.task_offsets = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| TaskOffset::sample(version, g)).collect() }) };
        _s.task_end_offsets = if g.bool() { None } else { Some({ let _k = g.array_len(); (0.._k).map(|_| TaskOffset::sample(version, g)).collect() }) };
        _s.shutdown_application = g.bool();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct KeyValue {
    /// key of the config
    pub key: String,
    /// value of the config
    pub value: String,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for KeyValue {
    fn default() -> Self {
        Self {
            key: String::new(),
            value: String::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for KeyValue {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.key, _flexible);
        _n += string_size(&self.value, _flexible);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.key, _flexible)?;
        put_string(buf, &self.value, _flexible)?;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let key = {
            get_string(buf, _flexible)?
        };
        let value = {
            get_string(buf, _flexible)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            key,
            value,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for KeyValue {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.key = g.string();
        _s.value = g.string();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct TopicInfo {
    /// The name of the topic.
    pub name: String,
    /// The number of partitions in the topic. Can be 0 if no specific number of partitions
    /// is enforced. Always 0 for changelog topics.
    pub partitions: i32,
    /// The replication factor of the topic. Can be 0 if the default replication factor
    /// should be used.
    pub replication_factor: i16,
    /// Topic-level configurations as key-value pairs.
    pub topic_configs: Vec<KeyValue>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for TopicInfo {
    fn default() -> Self {
        Self {
            name: String::new(),
            partitions: 0i32,
            replication_factor: 0i16,
            topic_configs: Vec::<KeyValue>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for TopicInfo {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.name, _flexible);
        _n += 4;
        _n += 2;
        _n += array_len_size(self.topic_configs.len(), _flexible);
        for _item in self.topic_configs.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.name, _flexible)?;
        put_i32(buf, self.partitions);
        put_i16(buf, self.replication_factor);
        put_array_len(buf, self.topic_configs.len(), _flexible)?;
        for _item in self.topic_configs.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let name = {
            get_string(buf, _flexible)?
        };
        let partitions = {
            get_i32(buf)?
        };
        let replication_factor = {
            get_i16(buf)?
        };
        let topic_configs = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            KeyValue::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            name,
            partitions,
            replication_factor,
            topic_configs,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for TopicInfo {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.name = g.string();
        _s.partitions = g.i32();
        _s.replication_factor = g.i16();
        _s.topic_configs = { let _k = g.array_len(); (0.._k).map(|_| KeyValue::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Endpoint {
    /// host of the endpoint
    pub host: String,
    /// port of the endpoint
    pub port: u16,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Endpoint {
    fn default() -> Self {
        Self {
            host: String::new(),
            port: 0u16,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Endpoint {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.host, _flexible);
        _n += 2;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.host, _flexible)?;
        put_u16(buf, self.port);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let host = {
            get_string(buf, _flexible)?
        };
        let port = {
            get_u16(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            host,
            port,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Endpoint {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.host = g.string();
        _s.port = g.u16();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct TaskOffset {
    /// The subtopology identifier.
    pub subtopology_id: String,
    /// The partition.
    pub partition: i32,
    /// The offset.
    pub offset: i64,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for TaskOffset {
    fn default() -> Self {
        Self {
            subtopology_id: String::new(),
            partition: 0i32,
            offset: 0i64,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for TaskOffset {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.subtopology_id, _flexible);
        _n += 4;
        _n += 8;
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.subtopology_id, _flexible)?;
        put_i32(buf, self.partition);
        put_i64(buf, self.offset);
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let subtopology_id = {
            get_string(buf, _flexible)?
        };
        let partition = {
            get_i32(buf)?
        };
        let offset = {
            get_i64(buf)?
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            subtopology_id,
            partition,
            offset,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for TaskOffset {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.subtopology_id = g.string();
        _s.partition = g.i32();
        _s.offset = g.i64();
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct TaskIds {
    /// The subtopology identifier.
    pub subtopology_id: String,
    /// The partitions of the input topics processed by this member.
    pub partitions: Vec<i32>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for TaskIds {
    fn default() -> Self {
        Self {
            subtopology_id: String::new(),
            partitions: Vec::<i32>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for TaskIds {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.subtopology_id, _flexible);
        _n += array_len_size(self.partitions.len(), _flexible);
        for _item in self.partitions.iter() {
            _n += 4;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.subtopology_id, _flexible)?;
        put_array_len(buf, self.partitions.len(), _flexible)?;
        for _item in self.partitions.iter() {
            put_i32(buf, *_item);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let subtopology_id = {
            get_string(buf, _flexible)?
        };
        let partitions = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i32(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            subtopology_id,
            partitions,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for TaskIds {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.subtopology_id = g.string();
        _s.partitions = { let _k = g.array_len(); (0.._k).map(|_| g.i32()).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Topology {
    /// The epoch of the topology. Used to check if the topology corresponds to the topology
    /// initialized on the brokers.
    pub epoch: i32,
    /// The sub-topologies of the streams application.
    pub subtopologies: Vec<Subtopology>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Topology {
    fn default() -> Self {
        Self {
            epoch: 0i32,
            subtopologies: Vec::<Subtopology>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Topology {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += 4;
        _n += array_len_size(self.subtopologies.len(), _flexible);
        for _item in self.subtopologies.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_i32(buf, self.epoch);
        put_array_len(buf, self.subtopologies.len(), _flexible)?;
        for _item in self.subtopologies.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let epoch = {
            get_i32(buf)?
        };
        let subtopologies = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            Subtopology::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            epoch,
            subtopologies,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Topology {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.epoch = g.i32();
        _s.subtopologies = { let _k = g.array_len(); (0.._k).map(|_| Subtopology::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Subtopology {
    /// String to uniquely identify the subtopology. Deterministically generated from the
    /// topology
    pub subtopology_id: String,
    /// The topics the topology reads from.
    pub source_topics: Vec<String>,
    /// The regular expressions identifying topics the subtopology reads from.
    pub source_topic_regex: Vec<String>,
    /// The set of state changelog topics associated with this subtopology. Created
    /// automatically.
    pub state_changelog_topics: Vec<TopicInfo>,
    /// The repartition topics the subtopology writes to.
    pub repartition_sink_topics: Vec<String>,
    /// The set of source topics that are internally created repartition topics. Created
    /// automatically.
    pub repartition_source_topics: Vec<TopicInfo>,
    /// A subset of source topics that must be copartitioned.
    pub copartition_groups: Vec<CopartitionGroup>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Subtopology {
    fn default() -> Self {
        Self {
            subtopology_id: String::new(),
            source_topics: Vec::<String>::new(),
            source_topic_regex: Vec::<String>::new(),
            state_changelog_topics: Vec::<TopicInfo>::new(),
            repartition_sink_topics: Vec::<String>::new(),
            repartition_source_topics: Vec::<TopicInfo>::new(),
            copartition_groups: Vec::<CopartitionGroup>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Subtopology {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += string_size(&self.subtopology_id, _flexible);
        _n += array_len_size(self.source_topics.len(), _flexible);
        for _item in self.source_topics.iter() {
            _n += string_size(&_item, _flexible);
        }
        _n += array_len_size(self.source_topic_regex.len(), _flexible);
        for _item in self.source_topic_regex.iter() {
            _n += string_size(&_item, _flexible);
        }
        _n += array_len_size(self.state_changelog_topics.len(), _flexible);
        for _item in self.state_changelog_topics.iter() {
            _n += _item.size(version);
        }
        _n += array_len_size(self.repartition_sink_topics.len(), _flexible);
        for _item in self.repartition_sink_topics.iter() {
            _n += string_size(&_item, _flexible);
        }
        _n += array_len_size(self.repartition_source_topics.len(), _flexible);
        for _item in self.repartition_source_topics.iter() {
            _n += _item.size(version);
        }
        _n += array_len_size(self.copartition_groups.len(), _flexible);
        for _item in self.copartition_groups.iter() {
            _n += _item.size(version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_string(buf, &self.subtopology_id, _flexible)?;
        put_array_len(buf, self.source_topics.len(), _flexible)?;
        for _item in self.source_topics.iter() {
            put_string(buf, &_item, _flexible)?;
        }
        put_array_len(buf, self.source_topic_regex.len(), _flexible)?;
        for _item in self.source_topic_regex.iter() {
            put_string(buf, &_item, _flexible)?;
        }
        put_array_len(buf, self.state_changelog_topics.len(), _flexible)?;
        for _item in self.state_changelog_topics.iter() {
            _item.encode(buf, version)?;
        }
        put_array_len(buf, self.repartition_sink_topics.len(), _flexible)?;
        for _item in self.repartition_sink_topics.iter() {
            put_string(buf, &_item, _flexible)?;
        }
        put_array_len(buf, self.repartition_source_topics.len(), _flexible)?;
        for _item in self.repartition_source_topics.iter() {
            _item.encode(buf, version)?;
        }
        put_array_len(buf, self.copartition_groups.len(), _flexible)?;
        for _item in self.copartition_groups.iter() {
            _item.encode(buf, version)?;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let subtopology_id = {
            get_string(buf, _flexible)?
        };
        let source_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        };
        let source_topic_regex = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        };
        let state_changelog_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TopicInfo::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let repartition_sink_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        };
        let repartition_source_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            TopicInfo::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let copartition_groups = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            CopartitionGroup::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            subtopology_id,
            source_topics,
            source_topic_regex,
            state_changelog_topics,
            repartition_sink_topics,
            repartition_source_topics,
            copartition_groups,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Subtopology {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.subtopology_id = g.string();
        _s.source_topics = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        _s.source_topic_regex = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        _s.state_changelog_topics = { let _k = g.array_len(); (0.._k).map(|_| TopicInfo::sample(version, g)).collect() };
        _s.repartition_sink_topics = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        _s.repartition_source_topics = { let _k = g.array_len(); (0.._k).map(|_| TopicInfo::sample(version, g)).collect() };
        _s.copartition_groups = { let _k = g.array_len(); (0.._k).map(|_| CopartitionGroup::sample(version, g)).collect() };
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct CopartitionGroup {
    /// The topics the topology reads from. Index into the array on the subtopology level.
    pub source_topics: Vec<i16>,
    /// Regular expressions identifying topics the subtopology reads from. Index into the
    /// array on the subtopology level.
    pub source_topic_regex: Vec<i16>,
    /// The set of source topics that are internally created repartition topics. Index into
    /// the array on the subtopology level.
    pub repartition_source_topics: Vec<i16>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for CopartitionGroup {
    fn default() -> Self {
        Self {
            source_topics: Vec::<i16>::new(),
            source_topic_regex: Vec::<i16>::new(),
            repartition_source_topics: Vec::<i16>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for CopartitionGroup {
    fn size(&self, version: i16) -> usize {
        let _flexible = true;
        let mut _n = 0usize;
        _n += array_len_size(self.source_topics.len(), _flexible);
        for _item in self.source_topics.iter() {
            _n += 2;
        }
        _n += array_len_size(self.source_topic_regex.len(), _flexible);
        for _item in self.source_topic_regex.iter() {
            _n += 2;
        }
        _n += array_len_size(self.repartition_source_topics.len(), _flexible);
        for _item in self.repartition_source_topics.iter() {
            _n += 2;
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = true;
        put_array_len(buf, self.source_topics.len(), _flexible)?;
        for _item in self.source_topics.iter() {
            put_i16(buf, *_item);
        }
        put_array_len(buf, self.source_topic_regex.len(), _flexible)?;
        for _item in self.source_topic_regex.iter() {
            put_i16(buf, *_item);
        }
        put_array_len(buf, self.repartition_source_topics.len(), _flexible)?;
        for _item in self.repartition_source_topics.iter() {
            put_i16(buf, *_item);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = true;
        let source_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i16(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let source_topic_regex = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i16(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let repartition_source_topics = {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_i16(buf)?
                        });
                    }
                    _v
                }
            }
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            source_topics,
            source_topic_regex,
            repartition_source_topics,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for CopartitionGroup {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = true;
        let mut _s = Self::default();
        _s.source_topics = { let _k = g.array_len(); (0.._k).map(|_| g.i16()).collect() };
        _s.source_topic_regex = { let _k = g.array_len(); (0.._k).map(|_| g.i16()).collect() };
        _s.repartition_source_topics = { let _k = g.array_len(); (0.._k).map(|_| g.i16()).collect() };
        _s
    }
}

impl ApiMessage for StreamsGroupHeartbeatRequest {
    const API_KEY: i16 = 88;
    const NAME: &'static str = "StreamsGroupHeartbeatRequest";
    const VALID: VersionRange = VersionRange::new(0, 0);
    const FLEXIBLE: VersionRange = VersionRange::new(0, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

