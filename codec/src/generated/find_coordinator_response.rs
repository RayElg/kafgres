// @generated by kafgres-codec-gen from FindCoordinatorResponse.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct FindCoordinatorResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota
    /// violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// The error code, or 0 if there was no error.
    pub error_code: i16,
    /// The error message, or null if there was no error.
    pub error_message: Option<String>,
    /// The node id.
    pub node_id: i32,
    /// The host name.
    pub host: String,
    /// The port.
    pub port: i32,
    /// Each coordinator result in the response.
    pub coordinators: Vec<Coordinator>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for FindCoordinatorResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            error_code: 0i16,
            error_message: Some(String::new()),
            node_id: 0i32,
            host: String::new(),
            port: 0i32,
            coordinators: Vec::<Coordinator>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for FindCoordinatorResponse {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 1 {
            _n += 4;
        }
        if version <= 3 {
            _n += 2;
        }
        if version >= 1 && version <= 3 {
            if version >= 1 && version <= 3 {
                _n += nullable_string_size(self.error_message.as_deref(), _flexible);
            } else if let Some(_v) = &self.error_message {
                _n += string_size(&_v, _flexible);
            }
        }
        if version <= 3 {
            _n += 4;
        }
        if version <= 3 {
            _n += string_size(&self.host, _flexible);
        }
        if version <= 3 {
            _n += 4;
        }
        if version >= 4 {
            _n += array_len_size(self.coordinators.len(), _flexible);
            for _item in self.coordinators.iter() {
                _n += _item.size(version);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 1 {
            put_i32(buf, self.throttle_time_ms);
        }
        if version <= 3 {
            put_i16(buf, self.error_code);
        } else {
            debug_assert!(self.error_code == 0i16, "FindCoordinatorResponse.ErrorCode: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 1 && version <= 3 {
            if version >= 1 && version <= 3 {
                put_nullable_string(buf, self.error_message.as_deref(), _flexible)?;
            } else {
                let _v = self.error_message.as_ref().ok_or(CodecError::UnexpectedNull("ErrorMessage"))?;
                put_string(buf, &_v, _flexible)?;
            }
        }
        if version <= 3 {
            put_i32(buf, self.node_id);
        } else {
            debug_assert!(self.node_id == 0i32, "FindCoordinatorResponse.NodeId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_string(buf, &self.host, _flexible)?;
        } else {
            debug_assert!(self.host == String::new(), "FindCoordinatorResponse.Host: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version <= 3 {
            put_i32(buf, self.port);
        } else {
            debug_assert!(self.port == 0i32, "FindCoordinatorResponse.Port: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_array_len(buf, self.coordinators.len(), _flexible)?;
            for _item in self.coordinators.iter() {
                _item.encode(buf, version)?;
            }
        } else {
            debug_assert!(self.coordinators == Vec::<Coordinator>::new(), "FindCoordinatorResponse.Coordinators: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let throttle_time_ms = if version >= 1 {
            get_i32(buf)?
        } else {
            0i32
        };
        let error_code = if version <= 3 {
            get_i16(buf)?
        } else {
            0i16
        };
        let error_message = if version >= 1 && version <= 3 {
            if version >= 1 && version <= 3 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            Some(String::new())
        };
        let node_id = if version <= 3 {
            get_i32(buf)?
        } else {
            0i32
        };
        let host = if version <= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let port = if version <= 3 {
            get_i32(buf)?
        } else {
            0i32
        };
        let coordinators = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            Coordinator::decode(buf, version)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<Coordinator>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            throttle_time_ms,
            error_code,
            error_message,
            node_id,
            host,
            port,
            coordinators,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for FindCoordinatorResponse {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 1 {
            _s.throttle_time_ms = g.i32();
        }
        if version <= 3 {
            _s.error_code = g.i16();
        }
        if version >= 1 && version <= 3 {
            _s.error_message = if version >= 1 && version <= 3 && g.bool() { None } else { Some(g.string()) };
        }
        if version <= 3 {
            _s.node_id = g.i32();
        }
        if version <= 3 {
            _s.host = g.string();
        }
        if version <= 3 {
            _s.port = g.i32();
        }
        if version >= 4 {
            _s.coordinators = { let _k = g.array_len(); (0.._k).map(|_| Coordinator::sample(version, g)).collect() };
        }
        _s
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Coordinator {
    /// The coordinator key.
    pub key: String,
    /// The node id.
    pub node_id: i32,
    /// The host name.
    pub host: String,
    /// The port.
    pub port: i32,
    /// The error code, or 0 if there was no error.
    pub error_code: i16,
    /// The error message, or null if there was no error.
    pub error_message: Option<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for Coordinator {
    fn default() -> Self {
        Self {
            key: String::new(),
            node_id: 0i32,
            host: String::new(),
            port: 0i32,
            error_code: 0i16,
            error_message: Some(String::new()),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for Coordinator {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version >= 4 {
            _n += string_size(&self.key, _flexible);
        }
        if version >= 4 {
            _n += 4;
        }
        if version >= 4 {
            _n += string_size(&self.host, _flexible);
        }
        if version >= 4 {
            _n += 4;
        }
        if version >= 4 {
            _n += 2;
        }
        if version >= 4 {
            if version >= 4 {
                _n += nullable_string_size(self.error_message.as_deref(), _flexible);
            } else if let Some(_v) = &self.error_message {
                _n += string_size(&_v, _flexible);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version >= 4 {
            put_string(buf, &self.key, _flexible)?;
        } else {
            debug_assert!(self.key == String::new(), "FindCoordinatorResponse.Key: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_i32(buf, self.node_id);
        } else {
            debug_assert!(self.node_id == 0i32, "FindCoordinatorResponse.NodeId: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_string(buf, &self.host, _flexible)?;
        } else {
            debug_assert!(self.host == String::new(), "FindCoordinatorResponse.Host: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_i32(buf, self.port);
        } else {
            debug_assert!(self.port == 0i32, "FindCoordinatorResponse.Port: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_i16(buf, self.error_code);
        } else {
            debug_assert!(self.error_code == 0i16, "FindCoordinatorResponse.ErrorCode: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            if version >= 4 {
                put_nullable_string(buf, self.error_message.as_deref(), _flexible)?;
            } else {
                let _v = self.error_message.as_ref().ok_or(CodecError::UnexpectedNull("ErrorMessage"))?;
                put_string(buf, &_v, _flexible)?;
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let key = if version >= 4 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let node_id = if version >= 4 {
            get_i32(buf)?
        } else {
            0i32
        };
        let host = if version >= 4 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let port = if version >= 4 {
            get_i32(buf)?
        } else {
            0i32
        };
        let error_code = if version >= 4 {
            get_i16(buf)?
        } else {
            0i16
        };
        let error_message = if version >= 4 {
            if version >= 4 {
                get_nullable_string(buf, _flexible)?
            } else {
                Some({
                    get_string(buf, _flexible)?
                })
            }
        } else {
            Some(String::new())
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            key,
            node_id,
            host,
            port,
            error_code,
            error_message,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for Coordinator {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version >= 4 {
            _s.key = g.string();
        }
        if version >= 4 {
            _s.node_id = g.i32();
        }
        if version >= 4 {
            _s.host = g.string();
        }
        if version >= 4 {
            _s.port = g.i32();
        }
        if version >= 4 {
            _s.error_code = g.i16();
        }
        if version >= 4 {
            _s.error_message = if version >= 4 && g.bool() { None } else { Some(g.string()) };
        }
        _s
    }
}

impl ApiMessage for FindCoordinatorResponse {
    const API_KEY: i16 = 10;
    const NAME: &'static str = "FindCoordinatorResponse";
    const VALID: VersionRange = VersionRange::new(0, 6);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

