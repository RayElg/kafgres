// @generated by kafgres-codec-gen from FindCoordinatorRequest.json
// Kafka 4.3.1. DO NOT EDIT — see codec/VENDORING.md.
#![allow(unused_variables, unused_mut, clippy::all)]

use crate::prelude::*;

#[derive(Debug, Clone, PartialEq)]
pub struct FindCoordinatorRequest {
    /// The coordinator key.
    pub key: String,
    /// The coordinator key type. (group, transaction, share).
    pub key_type: i8,
    /// The coordinator keys.
    pub coordinator_keys: Vec<String>,
    /// Tagged fields this build does not know. Kept so decode is forward-compatible.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for FindCoordinatorRequest {
    fn default() -> Self {
        Self {
            key: String::new(),
            key_type: 0i8,
            coordinator_keys: Vec::<String>::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl Encodable for FindCoordinatorRequest {
    fn size(&self, version: i16) -> usize {
        let _flexible = version >= 3;
        let mut _n = 0usize;
        if version <= 3 {
            _n += string_size(&self.key, _flexible);
        }
        if version >= 1 {
            _n += 1;
        }
        if version >= 4 {
            _n += array_len_size(self.coordinator_keys.len(), _flexible);
            for _item in self.coordinator_keys.iter() {
                _n += string_size(&_item, _flexible);
            }
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            _n += uvarint_size(_num_tagged);
            for _f in self.unknown_tagged_fields.iter() {
                _n += uvarint_size(_f.tag) + uvarint_size(_f.data.len() as u32) + _f.data.len();
            }
        }
        _n
    }

    fn encode(&self, buf: &mut BytesMut, version: i16) -> Result<(), CodecError> {
        let _flexible = version >= 3;
        if version <= 3 {
            put_string(buf, &self.key, _flexible)?;
        } else {
            debug_assert!(self.key == String::new(), "FindCoordinatorRequest.Key: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 1 {
            put_i8(buf, self.key_type);
        } else {
            debug_assert!(self.key_type == 0i8, "FindCoordinatorRequest.KeyType: non-ignorable field set at version {} that cannot carry it", version);
        }
        if version >= 4 {
            put_array_len(buf, self.coordinator_keys.len(), _flexible)?;
            for _item in self.coordinator_keys.iter() {
                put_string(buf, &_item, _flexible)?;
            }
        } else {
            debug_assert!(self.coordinator_keys == Vec::<String>::new(), "FindCoordinatorRequest.CoordinatorKeys: non-ignorable field set at version {} that cannot carry it", version);
        }
        if _flexible {
            let mut _num_tagged = self.unknown_tagged_fields.len() as u32;
            put_uvarint(buf, _num_tagged);
            let mut _ui = 0usize;
            while _ui < self.unknown_tagged_fields.len() {
                let _f = &self.unknown_tagged_fields[_ui];
                put_uvarint(buf, _f.tag);
                put_uvarint(buf, _f.data.len() as u32);
                buf.extend_from_slice(&_f.data);
                _ui += 1;
            }
        }
        Ok(())
    }

    fn decode(buf: &mut Bytes, version: i16) -> Result<Self, CodecError> {
        let _flexible = version >= 3;
        let key = if version <= 3 {
            get_string(buf, _flexible)?
        } else {
            String::new()
        };
        let key_type = if version >= 1 {
            get_i8(buf)?
        } else {
            0i8
        };
        let coordinator_keys = if version >= 4 {
            match get_array_len(buf, _flexible)? {
                None => return Err(CodecError::UnexpectedNull("array")),
                Some(_len) => {
                    let mut _v = alloc_vec(_len);
                    for _ in 0.._len {
                        _v.push({
                            get_string(buf, _flexible)?
                        });
                    }
                    _v
                }
            }
        } else {
            Vec::<String>::new()
        };
        let mut unknown_tagged_fields = Vec::new();
        if _flexible {
            let _num_tagged = get_uvarint(buf)?;
            let mut _last: Option<u32> = None;
            for _ in 0.._num_tagged {
                let _tag = get_uvarint(buf)?;
                if let Some(_l) = _last {
                    if _tag <= _l { return Err(CodecError::TagOutOfOrder { previous: _l, tag: _tag }); }
                }
                _last = Some(_tag);
                let _sz = get_uvarint(buf)? as usize;
                let mut _sub = take(buf, _sz)?;
                match _tag {
                    _ => unknown_tagged_fields.push(RawTaggedField { tag: _tag, data: _sub }),
                }
            }
        }
        Ok(Self {
            key,
            key_type,
            coordinator_keys,
            unknown_tagged_fields,
        })
    }
}

#[cfg(any(test, feature = "sample"))]
impl Sample for FindCoordinatorRequest {
    fn sample(version: i16, g: &mut Gen) -> Self {
        let _flexible = version >= 3;
        let mut _s = Self::default();
        if version <= 3 {
            _s.key = g.string();
        }
        if version >= 1 {
            _s.key_type = g.i8();
        }
        if version >= 4 {
            _s.coordinator_keys = { let _k = g.array_len(); (0.._k).map(|_| g.string()).collect() };
        }
        _s
    }
}

impl ApiMessage for FindCoordinatorRequest {
    const API_KEY: i16 = 10;
    const NAME: &'static str = "FindCoordinatorRequest";
    const VALID: VersionRange = VersionRange::new(0, 6);
    const FLEXIBLE: VersionRange = VersionRange::new(3, 32767);
    const LATEST_VERSION_UNSTABLE: bool = false;
}

